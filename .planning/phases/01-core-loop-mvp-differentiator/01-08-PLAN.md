---
phase: 01-core-loop-mvp-differentiator
plan: 8
type: execute
wave: 6
depends_on: [01-06, 01-07]
files_modified:
  - apps/e2e/src/seed.ts
  - apps/e2e/src/core-loop.test.ts
  - apps/e2e/playwright.config.ts
autonomous: true
requirements: [E2E-01]
user_setup: []

must_haves:
  truths:
    - "A user signs up, gets a personal org, creates a project, and prompts the mock AI — a proposal with generated cards appears"
    - "Approving the proposal makes cards live on the board; the card drawer shows version history"
    - "Closing a card freezes it; a follow-up instruction creates a replacement card (evolution) instead of updating it"
    - "A second user cannot see the first user's project (cross-org isolation)"
  artifacts:
    - path: "apps/e2e/src/core-loop.test.ts"
      provides: "The 4 Phase 1 journeys"
      exports: []
    - path: "apps/e2e/src/seed.ts"
      provides: "Extended seed (org + second user for isolation)"
      contains: "seedTestData"
  key_links:
    - from: "apps/e2e/playwright.config.ts"
      to: "AI_PROVIDER=mock"
      via: "api webServer env (deterministic AI, no live calls)"
      pattern: "AI_PROVIDER"
    - from: "apps/e2e/src/core-loop.test.ts"
      to: "apps/web data-testids"
      via: "prompt-input / approve-proposal / board-card / copy-link"
      pattern: "approve-proposal"
---

<objective>
Ship the Phase 1 E2E journeys with Playwright: signup → personal org → create project → prompt → mock AI generates cards → proposal → approve → board → card detail/versions → close card → replacement card → cross-user isolation — all deterministic via the mock AI provider.

Purpose: Proves the whole core loop works end-to-end and satisfies E2E-01. This is the final regression net for the milestone success criteria in ROADMAP Phase 1.
Output: Extended seed, 4 Playwright journeys in core-loop.test.ts, and mock-AI env wiring in the playwright config.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-loop-mvp-differentiator/01-PATTERNS.md (Group I E2E infra reuse)
@docs/superpowers/specs/2026-08-02-storyteller-design.md (§8 Phase 1 E2E journeys, §9 testing)
@apps/e2e/src/smoke.test.ts (journey test analog)
@apps/e2e/src/seed.ts (seed analog to extend)
@apps/e2e/playwright.config.ts (webServer env blocks)
@apps/e2e/src/global-setup.ts (test DB + migrations + seed flow)
@apps/web UI data-testids contract (plan 01-06 prompt-input, clarify-answer; plan 01-07 approve-proposal, board-card, copy-link, proposal-link)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the seed for the isolation journey + wire mock AI provider</name>
  <files>
    apps/e2e/src/seed.ts
    apps/e2e/playwright.config.ts
  </files>
  <read_first>
    apps/e2e/src/seed.ts (29 lines — user + account insert pattern)
    apps/e2e/playwright.config.ts (webServer array; api server env block)
  </read_first>
  <action>
    - seed.ts: keep the existing TEST_USER (User A). Add a second seeded user `TEST_USER_B` (email "e2e-b@test.com", password "TestPass123!") with its own account row — used by the isolation journey to prove User B cannot see User A's data. Do NOT pre-create orgs/projects in seed (the journeys create them through the UI — that's what E2E-01 verifies). If a journey needs deterministic board data, the mock AI provider (plan 01-02) already returns stable cards for the prompt "loyalty program" — rely on that rather than seeding cards.
    - playwright.config.ts: add to the api webServer env block: `AI_PROVIDER: "mock"` (and leave OPENAI_API_KEY unset). This makes every E2E run deterministic with no live API calls (design §9). Confirm the api webServer env already passes DATABASE_URL (it does — reuse getTestDatabaseUrl()).
  </action>
  <verify>
    <automated>cd apps/e2e && bun x tsc --noEmit 2>/dev/null; bun --filter e2e typecheck 2>/dev/null || bun run lint</automated>
  </verify>
  <acceptance_criteria>
    seed.ts inserts a second user B with account; playwright.config api server runs with AI_PROVIDER=mock; existing smoke tests still reference TEST_USER correctly; config + seed typecheck.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: The four Phase 1 E2E journeys</name>
  <files>
    apps/e2e/src/core-loop.test.ts
  </files>
  <read_first>
    apps/e2e/src/smoke.test.ts (page.goto + getByLabel/getByRole + toHaveURL patterns)
    apps/e2e/src/seed.ts (TEST_USER export)
    apps/web data-testids contract from plans 01-06/01-07 (prompt-input, clarify-answer, proposal-link, approve-proposal, reject-proposal, board-card, copy-link, history-tab, similar-list, close-card)
  </read_first>
  <action>
    Create apps/e2e/src/core-loop.test.ts with 4 journeys (each test creates fresh data via the UI; use fullyParallel: false for this file OR unique usernames per test to avoid collisions — prefer per-test unique slugs by appending Date.now()):

    J1 — "signup creates personal org, project, and AI-generated proposal" (AI-01, ORG-01, APPR-03):
      goto /signup → fill email/name/password (unique per run) → submit → expect redirect into the app (personal org exists — assert org switcher shows the personal org name or the /projects page renders) → click "New board" → create project (name unique) → land on /projects/:slug → open the chat panel (or the board's prompt input) → type a prompt mentioning "loyalty program" into [data-testid=prompt-input] → submit → expect a proposal to appear in the review queue ([data-testid=proposal-item] visible) OR clarifying questions ([data-testid=clarify-answer]) if the mock decides — the mock generate path returns the board for a substantive prompt, so assert the proposal path.

    J2 — "approving a proposal makes cards live with version history" (APPR-01/02, DATA-02, UI-04/05):
      continue from J1's proposal → click [data-testid=approve-proposal] → expect success toast + cards on the board ([data-testid=board-card] count > 0) → click a board card → drawer opens → History tab ([data-testid=history-tab]) shows version 1 (changeType "create") → Details tab shows the markdown body and acceptance criteria.

    J3 — "closing a card freezes it; a follow-up instruction creates a replacement" (AI-04, DATA-02, closed semantics):
      from the board, click a card → click close ([data-testid=close-card]) → confirm → the card moves to the Closed rail (visually distinct) → run a follow-up instruction in the chat targeting that card (e.g. "add gift cards to the loyalty program") → mock process returns a proposal whose change is a CREATE with an evolution relation (engine guard converts any closed-target update — plan 01-02) → approve → assert a NEW card exists (different slug/title) and the drawer's Relations tab shows an "evolution" edge from the closed card → assert the closed card's content is unchanged (still frozen).

    J4 — "cross-org isolation" (ORG-01/04, DATA-06, milestone criterion 4):
      after J1 created User A's project: sign out → sign in as TEST_USER_B (seeded) → goto /projects → assert User A's project name is NOT visible; directly goto /projects/<userA-project-slug> → expect 404/redirect (not-found) — User B must not see or access User A's project.

    Harden the journeys: unique emails per test (crypto.randomUUID or Date.now suffix) to avoid DB collisions on re-runs; use expect().toHaveURL with generous timeouts (10s) mirroring smoke.test.ts; use the data-testids from the UI plans; keep each journey to the core assertions above (no UI polish checks — those belong to manual UAT).

  </action>
  <verify>
    <automated>bun run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    core-loop.test.ts contains the 4 journeys; each journey is self-contained with unique-per-run identities; J3 asserts the replacement card + evolution edge and frozen closed card; J4 asserts User B cannot see or access User A's project; the full suite passes locally with AI_PROVIDER=mock (requires docker postgres + `bun dev` infrastructure per global-setup).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces (plan 08)

**Seed (apps/e2e/src/seed.ts):** TEST_USER_B (isolated second user + account).
**Config (apps/e2e/playwright.config.ts):** AI_PROVIDER=mock on the api webServer.
**Journeys (apps/e2e/src/core-loop.test.ts):** J1 signup→personal org→project→prompt→proposal; J2 approve→live cards→versions; J3 close→replacement+evolution; J4 cross-org isolation.
**Verification command:** `bun run test:e2e`.

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                       |
| ----------------- | ------------------------------------------------------------------------------------------------- |
| test runner → app | Playwright drives the real browser + real API — exercises the production security path, not mocks |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ---------------------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-01   | Spoofing               | seeded test users    | mitigate    | Seed users use fixed test credentials in a dedicated test DB (createTestDatabase drops/recreates per run) — no production data touched          |
| T-08-02   | Information disclosure | cross-org leakage    | mitigate    | J4 is an explicit regression journey: User B hitting User A's project slug must 404/redirect — asserts the org-scope middleware behavior (HIGH) |
| T-08-03   | Tampering              | test data collisions | mitigate    | Unique emails/slugs per test run (random suffix) so re-runs and parallel workers don't collide                                                  |
| T-08-04   | Injection              | mock AI determinism  | mitigate    | AI_PROVIDER=mock forces the deterministic provider — no live LLM output can flake or inject unexpected content into assertions                  |
| T-08-SC   | Tampering              | npm installs         | accept      | No new npm dependencies in this plan (Playwright already present)                                                                               |

</threat_model>

<verification>
- `bun run test:e2e` passes fully (requires: docker postgres running, `bun dev` web+api infra per global-setup, AI_PROVIDER=mock)
- The 4 journeys each end in the asserted state; J4 isolation assertion is green
- Existing smoke.test.ts still passes (regression)
</verification>

<success_criteria>

- E2E-01 delivered: the complete Phase 1 journey chain automated and passing with the mock AI provider
- Milestone success criteria 1-5 (ROADMAP Phase 1) are provable: personal org on signup, prompt→proposal→approve→board, version history, closed→replacement semantics, cross-org isolation
  </success_criteria>

<output>
Create `.planning/phases/01-core-loop-mvp-differentiator/01-08-SUMMARY.md` when done
</output>
