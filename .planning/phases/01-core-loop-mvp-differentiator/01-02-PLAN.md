---
phase: 01-core-loop-mvp-differentiator
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ai/package.json
  - packages/ai/tsconfig.json
  - packages/ai/src/types.ts
  - packages/ai/src/schemas.ts
  - packages/ai/src/errors.ts
  - packages/ai/src/providers/openai.ts
  - packages/ai/src/providers/anthropic.ts
  - packages/ai/src/providers/mock.ts
  - packages/ai/src/prompts/generate-board.ts
  - packages/ai/src/prompts/clarifying-questions.ts
  - packages/ai/src/prompts/process-instruction.ts
  - packages/ai/src/prompts/consistency-review.ts
  - packages/ai/src/operations/generate-board.ts
  - packages/ai/src/operations/clarify.ts
  - packages/ai/src/operations/process-instruction.ts
  - packages/ai/src/operations/consistency-review.ts
  - packages/ai/src/index.ts
  - packages/ai/src/__tests__/prompts.test.ts
  - packages/ai/src/__tests__/schemas.test.ts
  - packages/ai/src/__tests__/mock-provider.test.ts
  - packages/ai/src/__tests__/operations.test.ts
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, AI-06, AI-07, AI-08]
user_setup:
  - service: openai
    why: "Real (non-mock) AI generation. Not required — AI_PROVIDER=mock is the dev/test default with no keys."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI platform dashboard -> API keys"
      - name: EMBEDDING_MODEL
        source: "OpenAI model list (default text-embedding-3-small)"
      - name: CHAT_MODEL
        source: "OpenAI model list (default gpt-4o-mini)"

must_haves:
  truths:
    - "An LLMProvider interface with chat() and embed() exists with openai, anthropic (stub), and deterministic mock implementations selectable by AI_PROVIDER"
    - "Four prompt builders exist: generate-board, clarifying-questions, process-instruction, consistency-review"
    - "All AI JSON output is validated against strict Zod schemas; malformed output throws a friendly AiOutputError"
    - "AI operations return proposal objects and never write to the database"
    - "Processing an instruction that targets a closed card yields a create-replacement + evolution edge, never an update"
  artifacts:
    - path: "packages/ai/src/types.ts"
      provides: "LLMProvider + ChatMessage + BoardSnapshot + ProposalBatch types"
      exports: ["LLMProvider"]
    - path: "packages/ai/src/providers/mock.ts"
      provides: "Deterministic provider for tests/E2E"
      exports: ["createMockProvider"]
    - path: "packages/ai/src/operations/process-instruction.ts"
      provides: "Instruction → proposal batch with closed-card guard"
      exports: ["processInstruction"]
    - path: "packages/ai/src/schemas.ts"
      provides: "Strict AI output validation"
      exports: ["generateBoardOutputSchema"]
  key_links:
    - from: "packages/ai/src/index.ts"
      to: "providers/openai.ts|mock.ts|anthropic.ts"
      via: "AI_PROVIDER env selection (email package pattern)"
      pattern: "AI_PROVIDER"
    - from: "operations/process-instruction.ts"
      to: "schemas.ts"
      via: "Zod-validate provider output"
      pattern: "safeParse"
---

<objective>
Build the `@workspace/ai` package: a provider-agnostic AI engine that generates boards, asks clarifying questions, processes instructions into proposal batches, and runs consistency reviews — all validated against strict Zod schemas, never touching the database.

Purpose: This is the brain of the core loop (AI-01..08). The API layer (plan 01-05) calls these operations and persists the resulting proposals; the mock provider makes everything deterministic for unit tests and E2E.
Output: New package with LLMProvider interface, 3 providers, 4 prompt builders, 4 operations, strict output schemas, and unit tests.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-loop-mvp-differentiator/01-CONTEXT.md (AI Engine decisions)
@.planning/phases/01-core-loop-mvp-differentiator/01-PATTERNS.md (Group C: package scaffolding; Group H: tests)
@docs/superpowers/specs/2026-08-02-storyteller-design.md (AI Engine §5, env §11)
@packages/email/src/index.ts (provider selection + consoleSender analog for mock)
@packages/files/src/storage.ts (StorageProvider interface analog)
@packages/files/src/upload.ts (service module shape)
@packages/schemas/package.json (package.json analog)
@packages/files/src/index.ts (barrel analog)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the openai npm package is legitimate before install</name>
  <action>
    Blocking-human gate BEFORE any install: the human verifies the openai package on npmjs.com. The agent does not install openai until the human approves. Everything else in the plan (types, providers stubs, prompts, operations) may proceed in parallel, but the openai dependency must not be added to package.json before approval.
  </action>
  <what-built>
    Nothing yet — this is the package legitimacy gate. The `openai` SDK will be the only new npm dependency of @workspace/ai (the Anthropic provider is a stub with no SDK; zod/drizzle come from existing workspaces). The repo has no RESEARCH.md Package Legitimacy Audit table, so all packages are treated as [ASSUMED] and require human verification before install.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/openai
    2. Confirm the publisher is "openai" (OpenAI Inc.), weekly downloads are large (500k+), and the latest version is 4.x (matching the SDK used by the OpenAI Node quickstart).
    3. Reply "approved" to continue, or state concerns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold package + LLMProvider interface + provider implementations</name>
  <files>
    packages/ai/package.json
    packages/ai/tsconfig.json
    packages/ai/src/types.ts
    packages/ai/src/errors.ts
    packages/ai/src/providers/openai.ts
    packages/ai/src/providers/anthropic.ts
    packages/ai/src/providers/mock.ts
    packages/ai/src/index.ts
  </files>
  <read_first>
    packages/schemas/package.json (exports map + scripts shape; mirror with subpath exports "./providers/*", "./prompts/*", "./operations/*")
    packages/email/src/index.ts (lines 81-88: provider selection by env var; consoleSender as the deterministic analog; dynamic `await import(...)` for SDKs)
    packages/files/src/storage.ts (StorageProvider interface shape)
    packages/logger/src/server.ts (createLogger usage)
  </read_first>
  <action>
    Create the package at packages/ai/:
    - package.json: name "@workspace/ai", private, type module, main "./src/index.ts", exports map with "." plus "./providers/*", "./prompts/*", "./operations/*" subpaths (pattern from @workspace/schemas), scripts lint/format/typecheck/test/test:watch mirroring @workspace/schemas (vitest). Dependencies: "openai": pinned exact version (verified in Task 1), "zod" (workspace-consistent version), "@workspace/logger": "workspace:*". devDependencies: typescript ~6, @types/node. `.npmrc` save-exact already pinned at root.
    - tsconfig.json: extends "../../tsconfig.json", lib ES2023, noEmit true, include src.
    - types.ts: `export type ChatMessage = { role: "system" | "user" | "assistant"; content: string }`; `export interface LLMProvider { chat(messages: ChatMessage[]): Promise<string>; embed(texts: string[]): Promise<number[][]> }`; domain types: CardSnapshot { id, title, description, acceptanceCriteria: string[], status, priority, isClosed, epicName?, slug }, EpicSnapshot { id, name, order }, RelationSnapshot { sourceCardId, targetCardId, type }, BoardSnapshot { projectId, projectSlug, columns: string[], epics: EpicSnapshot[], cards: CardSnapshot[], relations: RelationSnapshot[] }, SemanticMatch { cardId, title, slug, isClosed, similarity }, ProposalChange types: CreateChange { changeType: "create", card: { title, description, acceptanceCriteria, status, priority, epicName?, customFields? }, relationSummary, conflictFlags }, UpdateChange { changeType: "update", targetCardId, fields: Partial<...>, relationSummary, conflictFlags }, CloseChange { changeType: "close", targetCardId, reason, relationSummary, conflictFlags }, ProposalBatch { changes: (CreateChange | UpdateChange | CloseChange)[] }.
    - errors.ts: `export class AiOutputError extends Error` with name "AiOutputError" and a friendly message carrying the underlying zod issue summary (constructor takes message + optional issues).
    - providers/openai.ts: createOpenAIProvider(env: { apiKey?, chatModel?, embeddingModel? }): LLMProvider — dynamic `await import("openai")`, chat() calls chat.completions.create with model = CHAT_MODEL default "gpt-4o-mini", embed() calls embeddings.create with model = EMBEDDING_MODEL default "text-embedding-3-small" mapping to number[][] via data[].embedding. Read model names from env with defaults; throw a clear error if no apiKey when the provider is constructed for real use (but allow construction — selection logic guards).
    - providers/anthropic.ts: createAnthropicProvider(): LLMProvider — STUB per design: both methods `throw new Error("Anthropic provider not implemented — Phase 1 ships the openai + mock providers")`. No SDK dependency.
    - providers/mock.ts: createMockProvider(): LLMProvider — fully deterministic, zero network:
      - embed(texts): returns for each text a deterministic 1536-dim vector seeded from a stable hash of the text (pseudo-random via a seeded PRNG, then normalized) — MUST be 1536 dims to match the card_embedding vector column; identical input → identical vector.
      - chat(messages): return the mock strategy: if the last user message mentions "clarif" or is under 20 chars → return the clarifying-questions JSON; if it mentions "process" or contains "add " → return the process-instruction JSON (a create change + an update change); if it mentions "review" → return the consistency-review JSON (a contradiction flag); otherwise → return the generate-board JSON (2 epics, 3 stories with STABLE slugs like "loyalty-enroll" / "loyalty-points-accrual" / "loyalty-rewards-catalog" — E2E depends on stable slugs). The JSON strings MUST be exactly conformant to the schemas in Task 3 (tested in mock-provider.test.ts). Keep the canned JSON in the module as template literals.
    - index.ts barrel: export { createOpenAIProvider, createAnthropicProvider, createMockProvider } + types + provider type. Provider selection lives HERE mirroring email/index.ts:81-88: `export const aiProvider: LLMProvider = process.env.AI_PROVIDER === "openai" ? createOpenAIProvider({ apiKey: process.env.OPENAI_API_KEY, chatModel: process.env.CHAT_MODEL, embeddingModel: process.env.EMBEDDING_MODEL }) : process.env.AI_PROVIDER === "anthropic" ? createAnthropicProvider() : createMockProvider()`. All reads are server-side only — never import process.env into anything that ships to the browser.
    Named exports only (no default exports) everywhere.
  </action>
  <verify>
    <automated>bun --filter @workspace/ai typecheck && bun --filter @workspace/ai lint</automated>
  </verify>
  <acceptance_criteria>
    package.json with subpath exports + pinned openai dep; LLMProvider interface exported; three providers implemented (openai via dynamic import, anthropic stub throwing, mock deterministic 1536-dim embeddings); aiProvider selected by AI_PROVIDER env with mock fallback; typecheck + lint clean; no default exports.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Strict AI output Zod schemas + four prompt builders</name>
  <files>
    packages/ai/src/schemas.ts
    packages/ai/src/prompts/generate-board.ts
    packages/ai/src/prompts/clarifying-questions.ts
    packages/ai/src/prompts/process-instruction.ts
    packages/ai/src/prompts/consistency-review.ts
  </files>
  <read_first>
    packages/logger/src/server.ts (module structure — named export per module)
    packages/schemas/src/validations/files.ts (constants style)
  </read_first>
  <action>
    schemas.ts — strict Zod output schemas (every object `.strict()`, AI-07). All exported with inferred types:
    - generateBoardOutputSchema: discriminatedUnion on "kind": { kind: "board", epics: array({ name: string 1..120, description: string, order: number int, stories: array({ title: 1..200, description: string, acceptanceCriteria: array(string), priority: enum low/medium/high/critical, suggestedStatus: enum backlog/todo/in_progress/review/done }) }) } | { kind: "clarifying", questions: array({ question: 1..300, options: array(string) optional }) }.
    - processInstructionOutputSchema: { changes: array(discriminatedUnion on change_type): create { change_type: literal "create", card: { title, description, acceptanceCriteria, status, priority, epicName optional, customFields optional record-of-string }, relation_summary: array({ type: enum dependency/hierarchy/evolution, source_card_id optional, target_card_id optional, note: string }), conflict_flags: array({ type: enum contradiction/duplicate/conflict, summary: string }) } | update { change_type: literal "update", target_card_id: string, fields: { title?, description?, acceptanceCriteria?, status?, priority?, customFields? } strict, relation_summary, conflict_flags } | close { change_type: literal "close", target_card_id: string, reason: string, relation_summary, conflict_flags } }.
    - consistencyReviewOutputSchema: { flags: array({ card_id: string, type: enum contradiction/duplicate/conflict, summary: 1..500 }) }.
    - clarifyingAnswersInputSchema: { answers: array({ question: string, answer: string }) } (input to the clarify op).
    Prompt builders (named exports, return ChatMessage[]; each composes a system prompt with the product rules + a user message with the input):
    - buildGenerateBoardPrompt({ prompt: string, existingContext?: string }): system = the product definition + "output JSON matching the generateBoard schema: either a board of epics/stories or clarifying questions when ambiguous"; user = the prompt.
    - buildClarifyingQuestionsPrompt({ question, answer, priorAnswers, prompt }): continues a Q&A exchange (threaded conversation).
    - buildProcessInstructionPrompt({ instruction, snapshot: BoardSnapshot, semanticMatches: SemanticMatch[] }): system = rules including "NEVER update a card with isClosed=true — instead emit a create replacement with an evolution relation"; user = compacted snapshot (cards: slug|status|isClosed|title, epics, relations) + semantic matches with open/closed flags + the instruction. This is the AI-04 guard at the prompt level.
    - buildConsistencyReviewPrompt({ snapshot, semanticMatches }): asks for contradiction/duplicate/conflict flags across cards.
    Keep prompts as pure string builders (no I/O). Include the required JSON shape hint (field names from schemas.ts) in each system prompt so providers return conformant output.
  </action>
  <verify>
    <automated>bun --filter @workspace/ai typecheck && bun --filter @workspace/ai test -- --run src/__tests__/prompts.test.ts src/__tests__/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    schemas.ts exports 4 strict output schemas + clarifyingAnswersInputSchema with inferred types; prompt builders are pure functions returning ChatMessage[]; process-instruction prompt explicitly forbids updating closed cards; schemas.test.ts proves strict rejection (extra keys fail) and valid samples pass; prompts.test.ts asserts prompt content includes the closed-card rule and the snapshot compaction.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: AI operations (generate-board, clarify, process-instruction, consistency-review) + tests</name>
  <files>
    packages/ai/src/operations/generate-board.ts
    packages/ai/src/operations/clarify.ts
    packages/ai/src/operations/process-instruction.ts
    packages/ai/src/operations/consistency-review.ts
    packages/ai/src/__tests__/mock-provider.test.ts
    packages/ai/src/__tests__/operations.test.ts
  </files>
  <read_first>
    packages/files/src/upload.ts (service shape: validate → call → return; no DB writes)
    packages/files/src/__tests__/upload.test.ts (vi.hoisted + vi.mock patterns)
  </read_first>
  <action>
    Operations (all pure: take provider + inputs, return typed results, NEVER write to DB — AI-08; the route layer persists):
    - generate-board.ts: `export async function generateBoard({ provider, prompt, snapshot }: { provider: LLMProvider; prompt: string; snapshot?: BoardSnapshot }): Promise<GenerateBoardResult>` where GenerateBoardResult = { kind: "board"; epics: EpicDraft[] } | { kind: "clarifying"; questions: ClarifyingQuestion[] }. Flow: buildGenerateBoardPrompt → provider.chat → zod safeParse → on failure throw AiOutputError with a friendly message (AI-07); map to camelCase result.
    - clarify.ts: `export async function answerClarifyingQuestions({ provider, prompt, question, answer, priorAnswers, snapshot }): Promise<GenerateBoardResult>` — threaded Q&A context, same validation.
    - process-instruction.ts: `export async function processInstruction({ provider, instruction, snapshot, semanticMatches }): Promise<ProposalBatch>` — buildProcessInstructionPrompt → chat → parse+validate → **defense-in-depth closed-card guard (AI-04)**: any returned update whose target_card_id resolves to a card with isClosed=true in the snapshot is converted to a create change (same title/description/fields) with an evolution relation_summary entry (type "evolution", source = the closed card id); any update targeting a card NOT in the snapshot is dropped with a warning logged. Return the cleaned batch.
    - consistency-review.ts: `export async function runConsistencyReview({ provider, snapshot, semanticMatches }): Promise<{ flags: ConsistencyFlag[] }>`.
    Tests (Vitest, analog upload.test.ts with vi.mock of @workspace/logger only — operations are pure):
    - mock-provider.test.ts: aiProvider defaults to mock with AI_PROVIDER unset; mock chat output parses against the 4 schemas (schemas.test.ts already covers strictness); mock embed returns 1536-dim normalized vectors and is deterministic (same text → same vector).
    - operations.test.ts: generateBoard with mock → 2 epics/3 stories with stable slugs; short/ambiguous prompt → clarifying kind; processInstruction with a snapshot containing a closed card → the returned batch has a create change with an evolution relation (never an update on the closed id); malformed provider output (stub provider returning "not json") → throws AiOutputError.
  </action>
  <verify>
    <automated>bun --filter @workspace/ai test</automated>
  </verify>
  <acceptance_criteria>
    4 operations exported and DB-free; processInstruction converts closed-card updates into create+evolution changes and drops unknown targets; malformed provider output throws AiOutputError with a friendly message; mock provider is deterministic and schema-conformant; all @workspace/ai tests pass (≥10 assertions).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces (plan 02)

**Package:** packages/ai (@workspace/ai) with subpath exports `./providers/*`, `./prompts/*`, `./operations/*`.
**Types (types.ts):** LLMProvider (chat/embed), ChatMessage, BoardSnapshot, CardSnapshot, EpicSnapshot, RelationSnapshot, SemanticMatch, ProposalBatch, CreateChange, UpdateChange, CloseChange, EpicDraft, ClarifyingQuestion, ConsistencyFlag.
**Errors (errors.ts):** AiOutputError.
**Providers:** createOpenAIProvider (dynamic openai SDK import; env OPENAI_API_KEY/CHAT_MODEL/EMBEDDING_MODEL), createAnthropicProvider (stub, throws), createMockProvider (deterministic 1536-dim embeddings + schema-conformant canned JSON with stable slugs).
**Selection:** aiProvider singleton (AI_PROVIDER env; mock fallback).
**Prompts (prompts/\*.ts):** buildGenerateBoardPrompt, buildClarifyingQuestionsPrompt, buildProcessInstructionPrompt, buildConsistencyReviewPrompt.
**Schemas (schemas.ts):** generateBoardOutputSchema, processInstructionOutputSchema, consistencyReviewOutputSchema, clarifyingAnswersInputSchema + inferred types.
**Operations (operations/\*.ts):** generateBoard, answerClarifyingQuestions, processInstruction (closed-card guard), runConsistencyReview.
**Tests:** mock-provider.test.ts, schemas.test.ts, prompts.test.ts, operations.test.ts.

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                              |
| -------------------- | -------------------------------------------------------------------------------------------------------- |
| provider → engine    | Untrusted LLM output (adversarial or malformed JSON) crosses here — strict schema validation is the gate |
| engine → route layer | Trusted typed results cross to plan 01-05 routes for persistence                                         |
| package install      | openai SDK from npm crosses into the supply chain                                                        |

## STRIDE Threat Register

| Threat ID | Category        | Component                                     | Disposition | Mitigation Plan                                                                                                                                                |
| --------- | --------------- | --------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Spoofing        | provider.chat output                          | mitigate    | Every operation Zod-safeParses provider output against .strict() schemas; AiOutputError with friendly message on malformed JSON (AI-07) — no silent acceptance |
| T-02-02   | Tampering       | mock provider canned JSON                     | mitigate    | Canned JSON is itself validated in tests (mock-provider.test.ts) so the mock never bypasses the schema path                                                    |
| T-02-03   | Info disclosure | OPENAI_API_KEY / CHAT_MODEL / EMBEDDING_MODEL | mitigate    | Env reads happen only in packages/ai server modules; nothing from @workspace/ai is imported by the web app; no API keys in any client bundle                   |
| T-02-04   | Elevation       | process-instruction batch                     | mitigate    | Closed-card guard converts update→create+evolution at the engine layer (AI-04); unknown target ids are dropped — enforced again at apply time in plan 01-05    |
| T-02-SC   | Tampering       | npm openai SDK                                | mitigate    | Blocking-human checkpoint (Task 1) verified the package publisher (openai) before install; version pinned exact per .npmrc save-exact                          |

</threat_model>

<verification>
- `bun --filter @workspace/ai typecheck && lint && test` all pass
- No default exports in the package (grep gate: `grep -v '^#' packages/ai/src -r --include="*.ts" | grep -c "export default"` == 0)
- No database imports in packages/ai (grep gate: no `@workspace/db` or `drizzle` import in packages/ai/src)
- Mock provider output passes the same strict schemas used for real providers
</verification>

<success_criteria>

- AI-01..AI-08 delivered: board generation, clarifying questions, instruction processing, closed-card replacement semantics, consistency review, pluggable providers (openai/mock), strict JSON validation, DB-free operations
- Mock provider deterministic: same prompt → same board JSON with stable slugs (E2E-ready)
- All unit tests green; package typechecks + lints
  </success_criteria>

<output>
Create `.planning/phases/01-core-loop-mvp-differentiator/01-02-SUMMARY.md` when done
</output>
