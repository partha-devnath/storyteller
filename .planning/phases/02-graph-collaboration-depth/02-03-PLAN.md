---
phase: 02-graph-collaboration-depth
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - apps/web/src/hooks/use-graph.ts
  - apps/web/src/lib/impact.ts
  - apps/web/src/lib/__tests__/impact.test.ts
  - apps/web/src/hooks/__tests__/use-graph.test.ts
  - apps/web/src/components/graph-node.tsx
  - apps/web/src/components/graph-edge.tsx
  - apps/web/src/components/graph-canvas.tsx
  - apps/web/src/components/graph-toolbar.tsx
  - apps/web/src/components/graph-view.tsx
  - apps/web/src/routes/project-board.tsx
autonomous: true
requirements: [UI-06]
user_setup: []

must_haves:
  truths:
    - "Graph tab renders an interactive node graph (pan/zoom/fitView) with custom nodes for epics and cards"
    - "Edges are color-coded by type: dependency amber, hierarchy sky, evolution violet"
    - "Edge filter toggles hide/show edge types client-side without refetch"
    - "Impact mode highlights the selected card plus all transitively downstream dependents and dims the rest"
    - "Clicking a card node opens the existing CardDrawer"
    - "Empty and error states render with the UI-SPEC copy and testids"
  artifacts:
    - path: "apps/web/src/hooks/use-graph.ts"
      provides: "Graph payload query + GraphNode/GraphEdge types"
      exports: ["useGraph", "GraphNode", "GraphEdge"]
    - path: "apps/web/src/lib/impact.ts"
      provides: "Pure downstream-impact computation"
      exports: ["computeImpact"]
    - path: "apps/web/src/components/graph-view.tsx"
      provides: "Lazy-loaded graph container (fetch + dagre layout + toolbar + canvas + states)"
      exports: ["GraphView"]
    - path: "apps/web/src/components/graph-node.tsx"
      provides: "Custom epic/card node rendering"
      exports: ["GraphNodeComponent"]
    - path: "apps/web/src/components/graph-edge.tsx"
      provides: "Custom type-colored edge rendering"
      exports: ["GraphEdgeComponent"]
  key_links:
    - from: "apps/web/src/components/graph-view.tsx"
      to: "GET /api/projects/:slug/graph"
      via: "useGraph fetch"
      pattern: "\\/graph"
    - from: "apps/web/src/components/graph-view.tsx"
      to: "apps/web/src/lib/impact.ts"
      via: "computeImpact on node click in impact mode"
      pattern: "computeImpact"
    - from: "apps/web/src/routes/project-board.tsx"
      to: "apps/web/src/components/graph-view.tsx"
      via: "React.lazy + Suspense conditional render on view=graph"
      pattern: "React\\.lazy|lazy\\("
---

<objective>
Build the graph view: fetch the graph payload, lay it out deterministically with dagre, render custom React Flow nodes/edges with type-colored strokes, add edge-type filter toggles, implement client-side impact highlighting, and integrate the whole thing into the project board as a lazy-loaded Graph tab.

Purpose: This is UI-06 — the phase's flagship feature. Every success criterion 1 and 2 item (nodes, colored edges, filter toggles, impact of X, drawer-on-click) lands here, matching the APPROVED UI-SPEC interaction contract exactly.

Output: use-graph hook, impact computation lib (pure + tested), five graph components, and the board integration with lazy loading.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-collaboration-depth/02-UI-SPEC.md
@apps/web/src/hooks/use-cards.ts
@apps/web/src/components/board-card.tsx
@apps/web/src/routes/project-board.tsx
@.planning/phases/01-core-loop-mvp-differentiator/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: use-graph hook + pure impact computation</name>
  <files>apps/web/src/hooks/use-graph.ts, apps/web/src/lib/impact.ts, apps/web/src/lib/__tests__/impact.test.ts, apps/web/src/hooks/__tests__/use-graph.test.ts</files>
  <read_first>
    - apps/web/src/hooks/use-cards.ts — the useQuery + apiClient pattern (queryKey conventions, enabled gating) this hook mirrors
    - 02-UI-SPEC.md — section V2 "Graph view" for the GraphNode/GraphEdge data contract and the impact traversal semantics ("reverse direction of dependency edges")
  </read_first>
  <action>
    Create apps/web/src/hooks/use-graph.ts — export types GraphNode and GraphEdge matching the 02-01 API contract exactly (GraphNode = { id, kind: "epic" | "card", title, subtitle: string | null, isClosed: boolean, priority: "low" | "medium" | "high" | "critical" | null, epicId: string | null, childCount: number }; GraphEdge = { id, source, target, type: "dependency" | "hierarchy" | "evolution" }). Export useGraph(projectSlug: string | undefined): useQuery with queryKey ["project", projectSlug, "graph"], queryFn fetching `/api/projects/${projectSlug}/graph` via apiClient, enabled: Boolean(projectSlug), returns { nodes, edges } (Envelope<{ nodes: GraphNode[]; edges: GraphEdge[] }>).

    Create apps/web/src/lib/impact.ts — export a PURE function computeImpact(nodes: GraphNode[], edges: GraphEdge[], selectedId: string | null): { nodeIds: Set<string>; edgeIds: Set<string> }. Semantics per UI-SPEC: edges render source → target where "source depends on target". Impact(X) = X plus every node transitively reachable by walking dependency edges in the REVERSE direction (start at X, follow edges whose target equals the current node, add their sources, repeat until no new nodes — these are the cards that transitively depend on X). Hierarchy/evolution edges are NOT traversed for reachability but ARE included in edgeIds when both their endpoints are in nodeIds. When selectedId is null, return empty sets. Return also a convenience array form if callers need it. No React imports — pure TS so it is trivially testable.

    Tests:
    - impact.test.ts: (a) direct dependency — A→B (A depends on B), select B → nodeIds {B, A}, edgeIds {A--B}; (b) transitive chain — A→B→C, select C → {C, B, A}; (c) hierarchy/evolution edges are excluded from traversal — H edge H→C plus dependency A→C, select C → {C, A} only, but edgeIds includes H--C when H is also impacted... (d) self-only when no dependents; (e) null selected → empty.
    - use-graph.test.ts: mock apiClient; assert queryKey is ["project", "demo", "graph"] and the query is disabled without a slug (follow the existing hook test mocking pattern; if none exists, assert via renderHook + QueryClientProvider per vitest/testing-library conventions).

  </action>
  <verify>
    <automated>bun --filter web test -- --run src/lib/__tests__/impact.test.ts src/hooks/__tests__/use-graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - impact.test.ts passes with RED-first ordering (write tests, see them fail, then implement, then green)
    - use-graph.test.ts asserts the query key and enabled gating
    - `bun --filter web typecheck` passes
    - grep `export default` in use-graph.ts and impact.ts returns 0
  </acceptance_criteria>
  <done>useGraph hook and pure computeImpact exist with passing tests; types match the API contract.</done>
</task>

<task type="auto">
  <name>Task 2: Custom graph node and edge components</name>
  <files>apps/web/src/components/graph-node.tsx, apps/web/src/components/graph-edge.tsx</files>
  <read_first>
    - 02-UI-SPEC.md — section V2 "Node shapes", "Edges", "Selected-node border contract", "A11y"; section "Color" (edge tokens + impact ring)
    - apps/web/src/components/board-card.tsx — the priorityClasses map to reuse verbatim (also check its export name so graph-node imports it)
    - 02-01-PLAN.md — GraphNode/GraphEdge field names the components consume
  </read_first>
  <action>
    Create apps/web/src/components/graph-node.tsx — export function GraphNodeComponent({ data, selected, isImpacted, dimmed }: { data: GraphNode; selected: boolean; isImpacted: boolean; dimmed: boolean }): renders a plain div (React Flow custom nodes can be HTML divs wrapped by the library — use the exported component shape React Flow expects, i.e. a component receiving { data, selected } from the flow; the isImpacted/dimmed values arrive via data or props depending on what graph-view passes through — pass them through data fields `data.isImpacted`, `data.dimmed` to keep the React Flow contract simple).

    Epic node (data.kind === "epic"): width 160px, `rounded-lg border-2 border-foreground/20 bg-card px-3 py-2`, a `Layers` lucide icon at 14px + name in `text-sm font-semibold line-clamp-2`, and a child-count badge `text-xs text-muted-foreground` rendering "{childCount} stories". No drawer on click (epic click is a no-op; see Task 3 wiring).

    Card node (data.kind === "card"): width 140px, `rounded-lg border bg-card px-3 py-2 shadow-sm`, title `text-sm font-semibold line-clamp-2` + priority chip reusing the priorityClasses map from board-card.tsx verbatim (import the existing map; do not duplicate it). Closed cards: `border-dashed opacity-60` + a `Lock` lucide icon at 12px inline.

    Impact styling contract (applies to both kinds): when data.isImpacted → `ring-2 ring-primary border-primary`; when data.dimmed → `opacity-25`; otherwise default borders (card: `border` (--border), epic: `border-2 border-foreground/20`). --primary border appears ONLY on impacted/selected nodes — never as a default outline.

    A11y: the node root is `<div role="button" tabIndex={0} aria-label={"Card: " + data.title} data-testid={"graph-node-" + data.id}>` and must forward click via onClick and Enter/Space keydown (React Flow wires node click; ensure the inner div calls the handlers React Flow passes in props.onClick / a custom onKeyDown). data-testid exactly `graph-node-{id}`.

    Create apps/web/src/components/graph-edge.tsx — export function GraphEdgeComponent({ source, target, type, selected, isImpacted, dimmed }): renders a React Flow BaseEdge (import { BaseEdge } from "@xyflow/react") with:
    - stroke class `stroke-edge-dependency` | `stroke-edge-hierarchy` | `stroke-edge-evolution` by type (Tailwind color tokens from 02-02)
    - stroke width: idle 1.5, hover 2.5, impacted 3 (data.isImpacted)
    - arrow marker in the same type color (define one markerEnd per type or one marker colored via the stroke; simplest: one arrow marker id per type using the token hex — derive from the oklch token by adding markers with markerEnd="url(#arrow-{type})" where markers are declared once in graph-view's svg defs — see Task 3)
    - rounded path (borderRadius 5)
    - data-testid `graph-edge-{source}--{target}` + attr data-edge-type={type} on the edge group
    - dimmed (data.dimmed) → `opacity-20`
    - Edge click is a no-op; hover via the library's edge hover state.

    Node/edge sizing: epic width 160, card width 140, both height ~56-64px — these fixed canvas units are the dagre layout inputs (Task 3).

  </action>
  <verify>
    <automated>bun --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter web typecheck` passes
    - grep `graph-node-` in graph-node.tsx returns ≥1 (the template literal uses `graph-node-${data.id}`); same for `graph-edge-` and `data-edge-type` in graph-edge.tsx
    - grep `font-medium` in graph-node.tsx and graph-edge.tsx returns 0
    - graph-node.tsx imports the priorityClasses map (grep `priorityClasses` returns ≥1) — reuse, not duplication
  </acceptance_criteria>
  <done>Custom node/edge components render the UI-SPEC shapes with correct tokens, testids, impact styling, and a11y roles.</done>
</task>

<task type="auto">
  <name>Task 3: graph-canvas, graph-toolbar, graph-view, and board integration</name>
  <files>apps/web/src/components/graph-canvas.tsx, apps/web/src/components/graph-toolbar.tsx, apps/web/src/components/graph-view.tsx, apps/web/src/routes/project-board.tsx</files>
  <read_first>
    - 02-UI-SPEC.md — sections V2 (toolbar, legend, impact banner, empty/error states, Controls), V1 (view switcher + export menu placement), "Component Inventory", and the E2E anchors table (graph-canvas, edge-filter-*, impact-toggle/banner/clear, graph-empty-cta, graph-retry)
    - apps/web/src/routes/project-board.tsx — the current board page structure this plan extends (header, grid, drawer state)
    - apps/web/src/components/view-switcher.tsx — the tab component created in 02-02 (read it to confirm its exact props/exports)
    - apps/web/src/components/kanban.tsx, closed-rail.tsx — what stays on the Board tab
  </read_first>
  <action>
    Create apps/web/src/components/graph-canvas.tsx — export function GraphCanvas({ nodes, edges, onNodeClick }: { nodes: Node[]; edges: Edge[]; onNodeClick: (cardId: string) => void }): a ReactFlow wrapper (import { ReactFlow, Controls } from "@xyflow/react") with:
    - nodeTypes/edgeTypes mapping the custom components (define the maps OUTSIDE the component to avoid re-creation each render)
    - `fitView` with padding 32, `nodesDraggable: true`, `proRanked` not required, `minZoom` default
    - Controls (zoom in/out/fit) bottom-left, no minimap
    - wrapper div data-testid="graph-canvas" with explicit height (e.g. h-[560px] or flex-1 within the page) so React Flow measures correctly
    - onNodeClick: only when the clicked node's kind is "card" → onNodeClick(node.id); epics no-op
    - The three arrow markers (`arrow-dependency`, `arrow-hierarchy`, `arrow-evolution`) defined in the svg defs once (React Flow renders markers from defs automatically when markerEnd matches)

    Create apps/web/src/components/graph-toolbar.tsx — export function GraphToolbar({ filters, onToggleFilter, impactArmed, onToggleImpact, hasNodes }: { filters: { dependency: boolean; hierarchy: boolean; evolution: boolean }; onToggleFilter: (type: "dependency" | "hierarchy" | "evolution") => void; impactArmed: boolean; onToggleImpact: () => void; hasNodes: boolean }): toolbar container `bg-muted/30 rounded-md px-3 py-2`, flex wrap gap-2:
    - three shadcn Toggle buttons: `edge-filter-dependency` "Dependency" (Link2 icon), `edge-filter-hierarchy` "Hierarchy" (GitBranch), `edge-filter-evolution` "Evolution" (GitFork) — pressed state styled `bg-primary text-primary-foreground`, default all pressed (filters all true)
    - impact Toggle: data-testid `impact-toggle`, Network icon, label "Impact", outline → active `bg-primary text-primary-foreground`; when armed show helper hint below the toolbar: "Select a card to see its downstream impact." (text-xs text-muted-foreground)
    - legend chips (text-xs): amber "Dependency", sky "Hierarchy", violet "Evolution" using the three edge color tokens (text-edge-* classes)
    - disabled/armed behavior: if !hasNodes, toggles can stay enabled but the view shows the empty state instead (see graph-view); keep the toolbar simple

    Create apps/web/src/components/graph-view.tsx — export function GraphView({ projectSlug, onSelectCard }: { projectSlug: string; onSelectCard: (cardId: string) => void }): the lazy-loaded container (the route lazy-imports this file — it must have a named export GraphView and be self-contained):
    - fetch via useGraph(projectSlug); loading → centered skeleton `animate-pulse bg-muted rounded-lg h-64`
    - build React Flow nodes/edges with useMemo: dagre layout (import dagre from "@dagrejs/dagre", rankdir "TB", nodeSize per kind: epic {w:160,h:64}, card {w:140,h:60}, ranksep/edgesep defaults) → deterministic positions (no force simulation); node data carries { ...GraphNode, isImpacted, dimmed }
    - client-side edge filtering: when a filter is off, drop edges of that type AND recompute the connected node set (UI-SPEC: "Toggling filters the rendered edges AND their connected node counts") — compute the visible node set as nodes incident to visible edges PLUS all epic nodes (epics always render so hierarchy context stays); apply as a useMemo
    - impact mode state: armed boolean + selectedId; when armed and a card node is clicked: compute via computeImpact (lib/impact.ts) → mark nodeIds as isImpacted (full opacity, ring) and the rest dimmed; edgeIds as isImpacted (stroke-width 3) and the rest opacity-20; render `impact-banner` above the canvas: "Showing impact of **{card title}**" (bold the title, text-sm) + ghost Clear button data-testid `impact-clear`; clicking the card in impact mode ALSO calls onSelectCard (drawer opens per UI-SPEC — impact highlight and drawer open together); exiting: impact-clear or impact-toggle off clears state
    - empty state (payload.nodes.length === 0): centered `py-24` stack — heading "No cards yet" (text-lg font-semibold), body "Generate your first story cards from the Chat tab, then view them here." (text-sm text-muted-foreground), CTA Button data-testid `graph-empty-cta` "Open Chat" that navigates to `/projects/{slug}/chat`
    - error state (query isError): banner in the canvas area "Couldn't load the graph." + Retry button data-testid `graph-retry` (refetch)
    - compose: <GraphToolbar /> above <GraphCanvas /> (plus impact banner and hints between them)

    Modify apps/web/src/routes/project-board.tsx:
    - import React.lazy + Suspense; `const GraphView = lazy(() => import("@/components/graph-view"))`
    - render <ViewSwitcher /> in a row above the board area (UI-SPEC V1: tabs above the board area)
    - read `view` from useSearchParams (default "board"); when "graph" → render <Suspense fallback={<div className="animate-pulse bg-muted rounded-lg h-64" />}><GraphView projectSlug={slug} onSelectCard={(id) => setActiveCardId(id)} /></Suspense> instead of the Kanban board + ClosedRail grid; keep the header (project name + card count), the drawer, and the ProposalReview panel on both tabs
    - the export menu and live indicator mount on the Board toolbar in 02-04 — do NOT add them here (file ownership: project-board.tsx is shared with 02-04; this plan only adds the view switcher + graph tab render)

  </action>
  <verify>
    <automated>bun --filter web typecheck && bun --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter web typecheck` and `bun --filter web build` pass
    - `bun --filter web lint` passes
    - grep `dagre` in graph-view.tsx returns ≥1 (layout uses dagre, not hand-positioning)
    - grep `React.lazy|lazy(` in project-board.tsx returns ≥1
    - grep the testids in the created files: graph-canvas (graph-canvas.tsx), impact-toggle + impact-banner + impact-clear (graph-toolbar.tsx / graph-view.tsx), graph-empty-cta + graph-retry (graph-view.tsx), edge-filter-dependency (graph-toolbar.tsx) all present
    - grep `font-medium` in the four new/modified files returns 0 (Phase 2 typography rule)
  </acceptance_criteria>
  <done>Graph tab renders the deterministic node graph with filters, impact mode, empty/error states, and drawer-on-click; board integrates it lazily.</done>
</task>

</tasks>

<artifact_production>

## Artifacts This Plan Produces

**New web files:**

- `apps/web/src/hooks/use-graph.ts` → `useGraph`, `GraphNode`, `GraphEdge`
- `apps/web/src/lib/impact.ts` → `computeImpact` (pure)
- `apps/web/src/components/graph-node.tsx` → `GraphNodeComponent`
- `apps/web/src/components/graph-edge.tsx` → `GraphEdgeComponent`
- `apps/web/src/components/graph-canvas.tsx` → `GraphCanvas`
- `apps/web/src/components/graph-toolbar.tsx` → `GraphToolbar`
- `apps/web/src/components/graph-view.tsx` → `GraphView` (lazy-loaded)

**Modified:**

- `apps/web/src/routes/project-board.tsx` (view switcher + lazy Graph tab)

**Test files:** `apps/web/src/lib/__tests__/impact.test.ts`, `apps/web/src/hooks/__tests__/use-graph.test.ts`
</artifact_production>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                             |
| ---------------------------- | ------------------------------------------------------------------------------------------------------- |
| web app→graph API            | Untrusted server data (node titles, epic names) rendered into DOM — must be treated as text, never HTML |
| user interaction→graph state | Node clicks drive impact computation — client-side only, no server writes                               |

## STRIDE Threat Register

| Threat ID | Category          | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                             |
| --------- | ----------------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-21   | Tampering         | Node/edge title rendering | mitigate    | All node/epic titles render as React text children ({data.title} in JSX) — React escapes by default; no dangerouslySetInnerHTML anywhere (Phase 1 grep gate re-checked after this plan: `grep -c dangerouslySetInnerHTML apps/web` stays 0) |
| T-02-22   | Info Disclosure   | Graph payload display     | mitigate    | Server already org-scopes (02-01); this plan renders only what the API returns; no client-side cross-org fetch paths                                                                                                                        |
| T-02-23   | Tampering         | dagre/react-flow parsing  | mitigate    | Layout consumes server-typed GraphNode/GraphEdge shapes; malformed payloads fail the query (apiClient throws on non-ok) and surface the error state instead of rendering                                                                    |
| T-02-24   | Denial of Service | Large graphs              | accept      | Boards are tens of nodes (UI-SPEC scale-fit rationale); computeImpact is O(V+E) BFS on in-memory arrays; if nodes exceed ~500 the UI-SPEC designates custom SVG as the fallback (future phase)                                              |
| T-02-SC   | Tampering         | npm installs              | accept      | No installs in this plan — deps came from 02-02's gated install                                                                                                                                                                             |

</threat_model>

<verification>
- `bun --filter web typecheck` passes
- `bun --filter web build` passes (vite build succeeds with the lazy chunk)
- `bun --filter web test` passes (impact + use-graph suites)
- `bun --filter web lint` passes
- Manual browser check (dev): open `/projects/{slug}?view=graph` with a seeded board → nodes render, filters toggle edge counts, impact highlights dependents, drawer opens on card click
- grep `dangerouslySetInnerHTML` in apps/web returns 0
</verification>

<success_criteria>

- Graph tab renders epics + cards as dagre-laid-out nodes with color-coded edges (amber/sky/violet)
- Filter toggles hide/show edge types and connected nodes instantly (no refetch)
- Impact mode: selecting a card highlights it + all transitive dependents with ring-2 ring-primary, dims others, shows the banner, and opens the drawer
- Empty and error states match the UI-SPEC copy with correct testids
- The Graph chunk ships only when the user opens the tab (React.lazy)
  </success_criteria>

<output>
Create `.planning/phases/02-graph-collaboration-depth/02-03-SUMMARY.md` when done
</output>
