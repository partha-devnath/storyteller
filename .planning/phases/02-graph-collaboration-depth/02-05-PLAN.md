---
phase: 02-graph-collaboration-depth
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-03, 02-04]
files_modified:
  - apps/e2e/src/seed.ts
  - apps/e2e/src/core-loop.test.ts
autonomous: true
requirements: [E2E-02]
user_setup: []

must_haves:
  truths:
    - "J1 — Graph: a seeded board renders epics + cards as nodes with at least one dependency, hierarchy, and evolution edge; filtering dependency edges off drops their count to 0; impact mode highlights >1 node with data-impact=true and shows the banner; clearing impact removes it; clicking a card node opens the drawer"
    - "J2 — Comments/mentions/SSE: typing @ opens the mention picker, selecting posts a comment with a mention chip, an API-posted comment arrives without reload (new-comments-pill), and the live indicator reports open"
    - "J3 — Export: CSV downloads with the expected header, JSON parses with nodes+edges, Markdown contains card titles"
  artifacts:
    - path: "apps/e2e/src/seed.ts"
      provides: "Seeded graph fixture: TEST_USER org + Graph Demo project + 2 epics + 5 cards (4 open, 1 closed) + dependency/hierarchy/evolution relations"
      exports: ["TEST_USER_ID", "TEST_USER_B_ID", "seedTestData"]
    - path: "apps/e2e/src/core-loop.test.ts"
      provides: "J1/J2/J3 Phase 2 journeys"
      contains: 'test.describe.serial("phase 2")'
  key_links:
    - from: "apps/e2e/src/core-loop.test.ts"
      to: "seed.ts graph fixture"
      via: "TEST_USER sign-in + Graph Demo project"
      pattern: "graph-demo"
    - from: "apps/e2e/src/core-loop.test.ts"
      to: "GET /api/cards/:id/comments (API post)"
      via: "page.request.post with ?project= query"
      pattern: "comments"
---

<objective>
Extend the E2E suite with the three Phase 2 journeys: J1 graph rendering/filtering/impact, J2 comments + @mentions + SSE live updates, and J3 export downloads — all against the isolated test DB with the mock AI provider, extending the proven apps/e2e/src/core-loop.test.ts pattern.

Purpose: E2E-02 is the acceptance proof for the entire phase. The journeys assert the UI-SPEC data-testid anchors contract end to end and catch regressions in the graph, collaboration, and export surfaces.

Output: A deterministic seeded graph fixture in seed.ts (2 epics, 5 cards, all three edge types) and three new Playwright journeys appended to core-loop.test.ts.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-loop-mvp-differentiator/01-08-SUMMARY.md
@apps/e2e/src/core-loop.test.ts
@apps/e2e/src/seed.ts
@apps/e2e/playwright.config.ts
@.planning/phases/02-graph-collaboration-depth/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the graph fixture (org + Graph Demo project + epics + cards + relations)</name>
  <files>apps/e2e/src/seed.ts</files>
  <read_first>
    - apps/e2e/src/seed.ts — current seed (insertUser for TEST_USER / TEST_USER_B only; providerId "credential"; crypto.randomUUID ids)
    - apps/e2e/src/prepare-test-db.ts — where seedTestData() is invoked (runs in the API webServer command, before the API boots)
    - packages/schemas/src/db/ — organization.ts, organization-member.ts, project.ts, epic.ts, card.ts, card-version.ts, card-relation.ts for exact columns (project.columns is a json column; card.acceptanceCriteria is json array; cardRelation.type is a string union)
  </read_first>
  <action>
    Modify apps/e2e/src/seed.ts:
    - Refactor insertUser to return the generated userId and store it: export const TEST_USER_ID and TEST_USER_B_ID (deterministic constants read by the journeys). Keep the credential-provider pattern unchanged.
    - Extend seedTestData() to build TEST_USER's org-scoped fixture. Steps (all direct Drizzle inserts via @workspace/db, mirroring the existing insertUser style; NO raw SQL):
      1. Org row (organization table): id uuid, name "E2E Org", createdBy/owner fields per the schema — read organization.ts first to match columns.
      2. organizationMember row: orgId, userId TEST_USER_ID, role "owner".
      3. Project row: id uuid, orgId, name "Graph Demo", slug "graph-demo" (deterministic), columns = the standard 5 (backlog/todo/in_progress/review/done) as a JSON array matching the project table's columns type, customFields [].
      4. Two epics: E1 "Enrollment" (order 0), E2 "Rewards" (order 1). E2 gets parentEpicId = E1 (exercises the parent-epic hierarchy edge). Epic ids must be exported as constants (EPIC_ENROLLMENT_ID, EPIC_REWARDS_ID) or derivable — export them for the journey assertions.
      5. Five cards (deterministic ids exported as CARD_* constants): C1 "Enrollment form" (epic E1, priority high, status todo), C2 "Loyalty points ledger" (epic E1, priority high, status in_progress), C3 "Rewards catalog" (epic E2, priority medium, status todo), C4 "Redemption flow" (epic E2, priority high, status review), C5 "Legacy rewards v1" (epic E2, isClosed true, priority low, status done) — C5 is the closed card that later evolves into C3 (or whichever replacement the fixture wants; pick: evolution relation C5 → C3).
      6. card_version v1 (changeType "create") for each of the 5 cards (the board/detail pages may expect versions).
      7. Relations (cardRelation): dependency C3 → C2 ("Rewards catalog depends on Loyalty points ledger"), dependency C4 → C3 ("Redemption flow depends on Rewards catalog") — a transitive chain so impact(C2) covers {C2, C3, C4} (reverse traversal); hierarchy C1 → C2 (card-level hierarchy relation row); evolution C5 → C3 (closed → replacement). All with projectId set.
    - Export a GRAPH_FIXTURE summary object (project slug, card ids, epic ids) or individual constants — the journey imports what it needs.
    - Keep TEST_USER_B and the Phase 1 behavior intact (smoke tests still pass).
  </action>
  <verify>
    <automated>bun --filter e2e typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter e2e typecheck` passes
    - `bun --filter e2e lint` passes
    - seed.ts exports TEST_USER_ID, TEST_USER_B_ID, and the graph fixture ids
    - A manual run of the existing suite still passes (J1-J4 core loop + 5 smoke tests) — `bun --filter e2e test` (requires `docker compose up -d postgres mailpit` first; this run validates the seed refactor did not break Phase 1)
  </acceptance_criteria>
  <done>Graph fixture seeds deterministically; Phase 1 journeys still green after the seed refactor.</done>
</task>

<task type="auto">
  <name>Task 2: J1 graph journey + J3 export journey</name>
  <files>apps/e2e/src/core-loop.test.ts</files>
  <read_first>
    - apps/e2e/src/core-loop.test.ts — the signIn helper, uniqueSuffix, and test.describe.serial("core loop") structure to extend; the UI-SPEC E2E anchors table for J1/J3 testids
    - 02-UI-SPEC.md — "E2E Visual Anchors" table (graph-canvas, graph-node-*, graph-edge-*, edge-filter-*, impact-toggle/banner/clear, export-menu/export-csv/json/markdown) and the J1/J3 journey descriptions
  </read_first>
  <action>
    Append to apps/e2e/src/core-loop.test.ts a new `test.describe.serial("phase 2", () => { ... })` block after the core-loop block. Import TEST_USER_ID and the graph fixture constants from "./seed". Reuse the existing signIn helper (module scope). Journey steps:

    J1 — Graph (seed-based, no prompt needed):
    1. signIn(page, TEST_USER.email, TEST_USER.password) — TEST_USER is seeded (emailVerified true); the fixture project "Graph Demo" (slug graph-demo) exists under TEST_USER's personal org.
    2. page.goto(`/projects/graph-demo?view=graph`) — deep link also proves the ?view= param path.
    3. expect view-switcher-graph to be visible; expect graph-canvas visible (timeout 15s).
    4. Assert nodes: `page.getByTestId(/^graph-node-/)` count ≥ 3 (2 epics + at least the card nodes that survive filtering; use a regex locator or count `page.locator('[data-testid^="graph-node-"]')`).
    5. Assert edges by type: `page.locator('[data-edge-type="dependency"]')` count ≥ 1; same for hierarchy and evolution (each ≥ 1) — the fixture guarantees one of each.
    6. Filter: click edge-filter-dependency → expect dependency edge count to drop to 0 (expect.poll on the locator count, timeout 5s) while hierarchy/evolution counts stay ≥ 1. Click again → dependency count back ≥ 1.
    7. Impact: click impact-toggle → click the node for C2 ("Loyalty points ledger" — use the fixture constant; locator `[data-testid="graph-node-{C2_ID}"]`). Expect impact-banner visible with text "Showing impact of Loyalty points ledger"; expect `[data-impact="true"]` count > 1 (C2 + C3 + C4 via reverse dependency traversal = 3); expect the banner's Clear button (impact-clear) → click → banner gone and data-impact count back to 0.
    8. Drawer: click the C1 node (plain card) → expect the CardDrawer title visible (getByText("Enrollment form") — the drawer opens; drawer presence via the card title text within the drawer container or an existing drawer anchor).
    NOTE on ids: card node data-testid uses the DB card id (uuid from seed constants) — use the exported constants to build the testids.

    J3 — Export (same fixture project, still signed in as TEST_USER):
    1. page.goto(`/projects/graph-demo`) (board tab).
    2. expect export-menu visible and enabled (the fixture has 5 cards).
    3. CSV: const dl = page.waitForEvent("download"); click export-csv; const download = await dl; const stream = await download.createReadStream(); read the full text; expect it contains the header `title,slug,status,priority,is_closed` and the title "Enrollment form".
    4. JSON: same download pattern on export-json → text parses with JSON.parse; parsed.nodes is an array with length ≥ 5 and parsed.edges contains at least one dependency-type edge.
    5. Markdown: same on export-markdown → text contains "# Graph Demo", the title "Redemption flow", and "(closed)" or "closed" marker for "Legacy rewards v1".
    (Playwright captures the download via Content-Disposition attachment responses; acceptDownloads is the default in modern Playwright — if the config lacks it, add `acceptDownloads: true` under use in playwright.config.ts and note the change.)

  </action>
  <verify>
    <automated>bun --filter e2e test -- src/core-loop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter e2e typecheck` passes
    - J1 passes: node count, one edge per type, dependency filter drops to 0 and restores, impact >1 with banner + clear, drawer opens on card click
    - J3 passes: CSV header + card title, JSON parses with nodes+edges, Markdown has project title + card titles + closed marker
    - No test relies on the mock AI prompt for graph data — the fixture is the source of truth
  </acceptance_criteria>
  <done>J1 and J3 pass against the seeded fixture with the UI-SPEC anchors.</done>
</task>

<task type="auto">
  <name>Task 3: J2 comments/mentions/SSE journey</name>
  <files>apps/e2e/src/core-loop.test.ts</files>
  <read_first>
    - 02-UI-SPEC.md — "E2E Visual Anchors" table (comment-input, comment-post, comment-item, comment-mention, mention-picker, mention-option-{userId}, live-indicator + data-status, new-comments-pill) and the J2 journey description ("create a comment via API from the test (simulating another user)")
    - apps/api/src/routes/cards.ts — POST /:id/comments requires the `?project=` query param (resolveOrgFromProject reads it) — the API-posted comment in J2 must include it
    - apps/e2e/src/core-loop.test.ts — the signIn helper to reuse
  </read_first>
  <action>
    Append J2 inside the same `test.describe.serial("phase 2")` block (runs after J1/J3 thanks to serial mode — reuse the signed-in session by re-running signIn for determinism):

    J2 — Comments/mentions/SSE (fixture project, TEST_USER):
    1. signIn(page, TEST_USER.email, TEST_USER.password) (idempotent).
    2. page.goto(`/projects/graph-demo`); expect live-indicator visible; expect.poll on `getByTestId("live-indicator").getAttribute("data-status")` → "open" (timeout 10s — the EventSource connects after page load).
    3. Open the drawer: click the board card "Enrollment form" (getByText within board-card or the card title locator) → drawer opens.
    4. Mention: click comment-input, type "Hi @" → expect mention-picker visible (timeout 5s) with at least one option `[data-testid="mention-option-{TEST_USER_ID}"]` (TEST_USER is the only member of his org) → click it → the textarea value now contains "@E2E User ".
    5. Post: type "check this" after the mention (fill appends or type), click comment-post → expect comment-item visible (timeout 10s) and comment-mention visible with text "@E2E User".
    6. SSE: post a second comment via the API from the test, simulating another user: `const res = await page.request.post('/api/cards/{C1_ID}/comments?project=graph-demo', { data: { body: 'Live comment from API', mentions: [] } })` — page.request shares the browser context's cookies so the session is authenticated; expect(res.ok()).toBeTruthy() (201).
    7. Without any reload: expect new-comments-pill to become visible (timeout 10s — the SSE event must arrive and the drawer must show the pill), and expect a comment-item with text "Live comment from API" to appear in the list (the SSE invalidation refetches the comments query).
    8. Optionally click the pill's Jump action → pill disappears (count cleared).
    Determinism note: if the SSE event races the poll, expect.poll with a generous timeout handles it; the API comment is created AFTER the page has an open EventSource (step 2 ensures data-status=open), so the event must be delivered.

  </action>
  <verify>
    <automated>bun --filter e2e test -- src/core-loop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - J2 passes: live indicator reaches open; mention picker shows the seeded member; posted comment renders a mention chip; API-posted comment surfaces without reload via new-comments-pill and the refreshed list
    - `bun --filter e2e test` (full suite: core-loop J1-J4 + phase-2 J1-J3 + 5 smoke tests) passes under the existing worker config
  </acceptance_criteria>
  <done>J2 proves the mention flow and SSE-driven live updates; the full E2E suite is green.</done>
</task>

</tasks>

<artifact_production>

## Artifacts This Plan Produces

**Modified:**

- `apps/e2e/src/seed.ts` → + `TEST_USER_ID`, `TEST_USER_B_ID`, graph fixture (E1/E2 epics, C1-C5 cards, dependency/hierarchy/evolution relations, org + membership + project rows)
- `apps/e2e/src/core-loop.test.ts` → + `test.describe.serial("phase 2")` with J1 (graph), J2 (comments/mentions/SSE), J3 (export)

**Possibly modified (only if needed):** `apps/e2e/playwright.config.ts` (acceptDownloads: true if not default)
</artifact_production>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                                       |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| E2E test→app UI             | Playwright drives the real browser against the real app + real API — the journeys exercise the production trust boundaries (auth, org scope, SSE) |
| E2E test→API (page.request) | The API-posted comment simulates a second user's action with the same session cookies — same org, same project, legitimate path                   |

## STRIDE Threat Register

| Threat ID | Category          | Component                | Disposition | Mitigation Plan                                                                                                                                                                                                                                                  |
| --------- | ----------------- | ------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-41   | Spoofing          | Seeded user sign-in      | mitigate    | Seed follows the Phase 1 pattern exactly (providerId "credential", @better-auth/utils/password scrypt hash, emailVerified true) — a fixture that cannot sign in proves nothing, so the smoke test still covers it                                                |
| T-02-42   | Info Disclosure   | Fixture scope            | mitigate    | All fixture rows are org-scoped under TEST_USER's org with projectId set on every row; the cross-org isolation J4 journey still passes (it asserts User B cannot see User A's data — the fixture adds rows to User A's org, strengthening the assertion surface) |
| T-02-43   | Tampering         | Test data pollution      | mitigate    | The isolated per-run test DB (name + \_e2e, created/migrated/seeded in the API webServer command, dropped in global teardown — 01-08 pattern) means the fixture can never touch the source DB                                                                    |
| T-02-44   | Denial of Service | SSE connections in tests | accept      | A few EventSource connections per test worker against localhost; the API's per-connection cleanup (02-01) bounds them; no rate limiter on /events by design (reconnects would 429 otherwise)                                                                     |
| T-02-SC   | Tampering         | npm installs             | accept      | No installs in this plan                                                                                                                                                                                                                                         |

</threat_model>

<verification>
- `bun --filter e2e typecheck` passes
- `bun --filter e2e lint` passes
- `bun --filter e2e test` passes — full suite: core-loop J1-J4, phase-2 J1-J3, 5 smoke tests (requires `docker compose up -d postgres mailpit` first; the API webServer command prepares the isolated test DB with AI_PROVIDER=mock)
- The seed refactor keeps Phase 1 journeys green (verified by the same full-suite run)
</verification>

<success_criteria>

- J1: nodes render, one edge per type, dependency filter drops to 0 and restores, impact highlights 3 nodes with banner + clear, drawer opens on card click
- J2: live indicator open, mention picker → chip → post, API comment surfaces via SSE without reload (new-comments-pill)
- J3: CSV header + titles, JSON parses (nodes+edges), Markdown contains project/card titles + closed marker
- All 12+ E2E tests pass against the isolated test DB with the mock AI provider
  </success_criteria>

<output>
Create `.planning/phases/02-graph-collaboration-depth/02-05-SUMMARY.md` when done
</output>
