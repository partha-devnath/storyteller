---
phase: 03-saas-hardening
plan: 06
type: execute
wave: 3
depends_on: [03-04]
files_modified:
  - apps/web/src/routes/onboarding.tsx
  - apps/web/src/components/template-card.tsx
  - apps/web/src/routes/analytics.tsx
  - apps/web/src/components/stat-card.tsx
  - apps/web/src/components/bar-chart.tsx
  - apps/web/src/components/__tests__/bar-chart.test.tsx
  - apps/web/src/components/__tests__/template-card.test.tsx
  - apps/web/src/routes/__tests__/analytics.test.tsx
autonomous: true
requirements: [E2E-03]
user_setup: []

must_haves:
  truths:
    - "/onboarding shows a 2-step flow (onboarding-welcome → onboarding-start → onboarding-template); 'Blank board' (onboarding-template-blank) creates an empty project via POST /api/projects and lands on the board; 'Sample — Product launch' (onboarding-template-product-launch) posts POST /api/orgs/:orgId/projects/template and lands on the seeded board; 'Skip for now' (onboarding-skip) navigates to /projects without re-triggering this session"
    - "/orgs/:orgId/analytics renders 4 stat cards (analytics-stat-{metric}) with values from the API, custom SVG bar charts (analytics-chart-{metric} + analytics-bar-{metric}-{index} with data-value), an empty state (analytics-empty-cta) for zero activity, and an error state (analytics-retry)"
  artifacts:
    - path: "apps/web/src/routes/onboarding.tsx"
      provides: "2-step onboarding flow"
      exports: ["OnboardingPage"]
    - path: "apps/web/src/components/template-card.tsx"
      provides: "Single template option card"
      exports: ["TemplateCard"]
    - path: "apps/web/src/routes/analytics.tsx"
      provides: "Analytics dashboard body"
      exports: ["AnalyticsPage"]
    - path: "apps/web/src/components/bar-chart.tsx"
      provides: "Custom SVG bar chart (no recharts)"
      exports: ["BarChart"]
  key_links:
    - from: "apps/web/src/routes/onboarding.tsx"
      to: "POST /api/projects (blank) + POST /api/orgs/:orgId/projects/template (sample)"
      via: "template-card onUseTemplate"
      pattern: "apiClient|useCreateProject|template"
    - from: "apps/web/src/routes/analytics.tsx"
      to: "useAnalytics"
      via: "stat cards + charts"
      pattern: "useAnalytics"
    - from: "apps/web/src/components/bar-chart.tsx"
      to: "series data"
      via: "rect per point with title + data-value"
      pattern: "analytics-bar|data-value|<title>"
---

<objective>
Build the onboarding flow (2-step: welcome → template pick) and the analytics dashboard (stat cards + custom SVG bar charts + empty/error states) per the approved UI-SPEC V3/V5.

Purpose: Success criterion 3 — onboarding and analytics dashboards available. Interface-first: this plan builds against the 03-01 types + 03-04 hooks (useAnalytics, useOnboarding) + the UI-SPEC API contracts, in the same wave as the analytics/template endpoints (03-03); component tests mock the data layer.

Output: Onboarding route + template-card component, analytics route + stat-card + bar-chart components, and component tests covering the chart/empty/error contracts.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-saas-hardening/03-01-SUMMARY.md
@.planning/phases/03-saas-hardening/03-04-SUMMARY.md
@.planning/phases/03-saas-hardening/03-UI-SPEC.md
@apps/web/src/routes/onboarding.tsx
@apps/web/src/routes/analytics.tsx
@apps/web/src/hooks/use-analytics.ts
@apps/web/src/hooks/use-onboarding.ts
@apps/web/src/hooks/use-projects.ts
@packages/schemas/src/plans.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding flow — welcome + template pick + blank/sample/skip actions</name>
  <files>apps/web/src/routes/onboarding.tsx, apps/web/src/components/template-card.tsx, apps/web/src/components/__tests__/template-card.test.tsx</files>
  <read_first>
    - apps/web/src/routes/onboarding.tsx (03-04 stub) — replace the body, keep named export OnboardingPage
    - 03-UI-SPEC.md — V3 full contract (step indicator "1 of 2", headings, body copy, template cards copy, skip behavior) + E2E anchors (onboarding-welcome/onboarding-start/onboarding-template/onboarding-skip/onboarding-template-blank/onboarding-template-product-launch)
    - apps/web/src/hooks/use-onboarding.ts — dismissOnboarding (called on skip) + isOnboardingSkipped
    - apps/web/src/hooks/use-projects.ts — useCreateProject (blank board path) signature
    - apps/web/src/stores/app-store.ts or board-store — selectedOrgId source for the template endpoint URL
  </read_first>
  <action>
    Replace apps/web/src/routes/onboarding.tsx (named export `OnboardingPage`):
    - Local state step: 1 | 2. Step indicator "1 of 2" / "2 of 2" (12px muted) — no progress bar (UI-SPEC V3).
    - Layout: page under the app shell, `mx-auto max-w-md space-y-8 py-12` centered column.
    - Step 1 (data-testid="onboarding-welcome"): Heading 24px/600 "Welcome to Storyteller" (fall back to "Welcome to {orgName}" when the user has orgs but zero projects — derive orgName from useOrgs first org). Body 14px/400: "Turn a product idea into a living requirements board — AI generates, reviews, and keeps your stories in sync." Primary Button "Get started" (data-testid="onboarding-start") → setStep(2).
    - Step 2 (data-testid="onboarding-template"): Heading "How would you like to start?" + body "Pick a template or begin with a blank board." (14px). Template list (2-col grid / horizontal list of Cards):
      - Blank board (data-testid="onboarding-template-blank"): TemplateCard { icon: FilePlus (lucide 14px), name: "Blank board", description: "Start from scratch with an empty board." } → onUseTemplate: useCreateProject().mutateAsync({ orgId, name: "Untitled board", slug }) then navigate(`/projects/${slug}`). Pick a stable default name "Untitled board" (server slugifies).
      - Sample — Product launch (data-testid="onboarding-template-product-launch"): TemplateCard { icon: Rocket, name: "Sample — Product launch", description: "A sample requirements board seeded with example epics and stories." } → POST `/api/orgs/${orgId}/projects/template` body { templateId: "product-launch" } via apiClient, then navigate(`/projects/${slug}`).
      - orgId = selectedOrgId ?? first org id (useOrgs).
    - Loading: 2 skeleton cards (animate-pulse bg-muted rounded-lg h-24) while the org list loads.
    - Failure: inline error under the cards "Couldn't create your board. Try again." (12px destructive) + buttons re-enable.
    - Skip: ghost link "Skip for now" (data-testid="onboarding-skip", 14px) → dismissOnboarding() + navigate("/projects"). No re-trigger this session (protected-route guard reads the session key from 03-04).

    Create apps/web/src/components/template-card.tsx (named export `TemplateCard`, props { icon: LucideIcon; name: string; description: string; onUseTemplate: () => void; pending?: boolean }):
    - Card p-4 with icon (14px), name (14px/600), description (12px text-muted-foreground), primary sm Button "Use template" (data-testid prop passed via parent wrapper, disabled + "Creating…" when pending).

    Tests (template-card.test.tsx): renders name/description/icon + "Use template" button; click fires onUseTemplate; pending shows "Creating…" and disables.
  </action>
  <verify>
    <automated>bun --filter web test && bun --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter web test` passes (template-card suite)
    - `bun --filter web typecheck` passes
    - onboarding.tsx contains onboarding-welcome/onboarding-start/onboarding-template/onboarding-skip/onboarding-template-blank/onboarding-template-product-launch testids (grep each)
    - Blank path calls useCreateProject; sample path POSTs /api/orgs/:orgId/projects/template with templateId "product-launch" (grep both)
    - Skip calls dismissOnboarding + navigates to /projects
    - Failure state renders "Couldn't create your board. Try again."
  </acceptance_criteria>
  <done>Onboarding 2-step flow works: blank + sample creation, skip without loop, error state handled.</done>
</task>

<task type="auto">
  <name>Task 2: Analytics dashboard — stat cards + custom SVG bar charts + empty/error states</name>
  <files>apps/web/src/routes/analytics.tsx, apps/web/src/components/stat-card.tsx, apps/web/src/components/bar-chart.tsx, apps/web/src/components/__tests__/bar-chart.test.tsx, apps/web/src/routes/__tests__/analytics.test.tsx</files>
  <read_first>
    - apps/web/src/routes/analytics.tsx (03-04 stub) — replace the body, keep named export AnalyticsPage
    - 03-UI-SPEC.md — V5 full contract: stat cards (analytics-stat-{metric}, values 24px/600 + labels 12px muted), bar-chart contract (viewBox ~560x160, rect fill = fixed metric color from chart-1..4 mapping, rx=2, gap >= 2px, native <title> tooltips, aria-hidden, data-value), empty state copy + CTA, error state; chart color mapping (chart-1 cardsCreated, chart-2 proposalsApproved, chart-3 commentsPosted, chart-4 members active)
    - apps/web/src/hooks/use-analytics.ts — query shape
    - packages/schemas/src/plans.ts — AnalyticsState type
  </read_first>
  <action>
    Create apps/web/src/components/stat-card.tsx (named export `StatCard`, props { value: number; label: string; testId: string }):
    - Card p-4: value 24px/600, label 12px text-muted-foreground. data-testid={testId}.

    Create apps/web/src/components/bar-chart.tsx (named export `BarChart`, props { title: string; data: { date: string; value: number }[]; colorVar: "chart-1" | "chart-2" | "chart-3"; testId: string }):
    - Card p-4 with title (14px/600) + caption "Last 30 days" (12px muted).
    - SVG: viewBox fixed "0 0 560 160", aria-hidden="true", data-testid={testId} (e.g. analytics-chart-cardsCreated). For each point i: rect with x computed from series length (bar width = 560/n - gap, gap >= 2px), y = height - valueScaled (scale max value to 140px bar area), height = valueScaled, rx=2, fill = var(--chart-1/2/3) — use inline style={{ fill: "var(--chart-1)" }} with the colorVar mapping, data-testid={`analytics-bar-{metric}-{i}`}, data-value={value}, and a native <title>{date}: {value}</title> child. Zero-value points render a 2px stub bar (visible baseline). All-zero series → parent renders the empty state instead (no chart).
    - No axes/ticks (UI-SPEC V5b — charts stay simple).

    Replace apps/web/src/routes/analytics.tsx (named export `AnalyticsPage`):
    - orgId from useParams; analytics = useAnalytics(orgId). Page title "Analytics" (text-2xl font-semibold), space-y-6.
    - Loading: 4 skeleton stat cards (`animate-pulse bg-muted rounded-lg h-20` ×4) + a skeleton chart block.
    - Error: banner "Couldn't load analytics." + Retry Button (data-testid="analytics-retry") refetching.
    - Empty (all totals 0 or series all-zero): centered stack py-24 — heading "No activity yet" (18px/600), body "Generate your first story cards, approve a proposal, or post a comment — your team's usage stats will appear here." (14px/400), CTA "Open Chat" (data-testid="analytics-empty-cta", navigates to /projects).
    - Data: grid gap-4 sm:grid-cols-2 lg:grid-cols-4 of StatCard: analytics-stat-cardsCreated "Cards created", analytics-stat-proposalsApproved "Proposals approved", analytics-stat-commentsPosted "Comments posted", analytics-stat-activeMembers "Active members" (value from totals). Below: one BarChart per series (cardsCreated → colorVar chart-1, proposalsApproved → chart-2, commentsPosted → chart-3) with the matching testid.

    Tests:
    - bar-chart.test.tsx: renders n rects for n points with data-testid analytics-bar-{metric}-{i} + data-value; each rect has a <title> child; all-zero input → renders nothing (parent handles empty).
    - analytics.test.tsx: mocked useAnalytics data → 4 stat cards with values; empty state renders analytics-empty-cta when totals are zero; error state renders analytics-retry.
  </action>
  <verify>
    <automated>bun --filter web test && bun --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter web test` passes (bar-chart + analytics suites)
    - `bun --filter web typecheck` passes
    - analytics.tsx renders analytics-stat-{metric} ×4 + analytics-chart-{metric} ×3 + analytics-empty-cta + analytics-retry (grep each)
    - bar-chart rects carry data-value + <title>; chart svg is aria-hidden (grep)
    - Fill colors use var(--chart-1/2/3) tokens only (no hardcoded hex) — grep "var(--chart-"
    - Empty state uses the exact V5c copy
  </acceptance_criteria>
  <done>Analytics dashboard renders stats + custom SVG charts + empty/error states with the exact anchor contract.</done>
</task>

</tasks>

<artifact_production>

## Artifacts This Plan Produces

**New components (apps/web/src/components/):**

- `template-card.tsx` → `TemplateCard` (icon/name/description + "Use template")
- `stat-card.tsx` → `StatCard` (value + label)
- `bar-chart.tsx` → `BarChart` (custom SVG, per-series token color, title tooltips)

**Modified:**

- `apps/web/src/routes/onboarding.tsx` (2-step flow replacing the 03-04 stub)
- `apps/web/src/routes/analytics.tsx` (dashboard body replacing the 03-04 stub)

**Test files:** `components/__tests__/bar-chart.test.tsx`, `components/__tests__/template-card.test.tsx`, `routes/__tests__/analytics.test.tsx`
</artifact_production>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → template endpoint | Creating a project/cards via onboarding template — role-gated + project-limit at the API (03-03) |
| API analytics → charts | Rendered values come from org-scoped aggregation (03-03); charts are presentational |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-50 | Tampering | template creation | accept | UI calls the authenticated endpoint only; server validates templateId enum + projects limit (03-03) |
| T-03-51 | Spoofing | chart data | accept | Rendered from useAnalytics server payload; aria-hidden + stat cards carry accessible values (UI-SPEC V5b a11y contract) |
| T-03-52 | DoS | chart rendering | accept | Fixed 30 points max (30d); SVG size bounded (viewBox 560x160) |
| T-03-SC | Tampering | npm installs | accept | No installs — custom SVG charts (recharts explicitly rejected in UI-SPEC Registry Safety) |

</threat_model>

<verification>
- `bun --filter web test` passes (bar-chart + template-card + analytics suites)
- `bun --filter web typecheck` + `lint` pass
- `bun run typecheck` passes repo-wide
- Manual smoke (after 03-03): fresh-user signup → /onboarding renders; seeded-activity org → /orgs/:orgId/analytics shows stats + charts
</verification>

<success_criteria>

- Onboarding: welcome → template → blank creates "Untitled board" and lands on it; sample creates the product-launch board; skip lands on /projects with no loop
- Analytics: 4 stat cards + 3 SVG charts with token colors + native title tooltips; empty + error states per contract
- All anchors from the UI-SPEC E2E table present (O1/A1 journeys ready in 03-07)
</success_criteria>

<output>
Create `.planning/phases/03-saas-hardening/03-06-SUMMARY.md` when done
</output>
