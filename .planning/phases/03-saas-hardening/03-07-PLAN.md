---
phase: 03-saas-hardening
plan: 07
type: execute
wave: 4
depends_on: [03-02, 03-03, 03-05, 03-06]
files_modified:
  - apps/e2e/src/seed.ts
  - apps/e2e/src/saas.test.ts
autonomous: true
requirements: [E2E-03]
user_setup: []

must_haves:
  truths:
    - "B1 — Billing page: navigate to Billing → current-plan shows Free + current-plan-badge on plan-card-free → plan-select-pro visible → click → navigates to the mock checkout url → usage meters render with data-pct"
    - "B2 — Plan-limit enforcement: seeded over-limit org → limit-banner visible with data-limit-metric → 'New board' disabled with limit-tooltip → usage-bar-{metric} at 100% → limit-banner-upgrade navigates to Billing"
    - "B3 — Downgrade: pro-seeded org → plan-select-free opens plan-change-dialog → cancel closes without change → confirm → success toast + plan flips to Free"
    - "O1 — Onboarding: fresh user (zero projects) redirected to /onboarding → welcome → start → template → blank creates a project and lands on the board; a second fresh user skips → lands on /projects without a loop"
    - "A1 — Analytics: seeded activity org → analytics-stat-* values > 0 → analytics-chart-* svgs with >= 1 analytics-bar-* per metric; fresh org → analytics-empty-cta"
  artifacts:
    - path: "apps/e2e/src/seed.ts"
      provides: "Phase 3 fixtures: subscription rows (free over-limit org + pro org), activity rows (cards/proposals/comments with dates), fresh user"
      exports: ["FREE_ORG_ID", "PRO_ORG_ID", "seedPhase3Fixtures"]
    - path: "apps/e2e/src/saas.test.ts"
      provides: "B1/B2/B3/O1/A1 journeys"
      contains: 'test.describe.serial("phase 3")'
  key_links:
    - from: "apps/e2e/src/saas.test.ts"
      to: "seed.ts phase 3 fixtures"
      via: "TEST_USER sign-in + fixture orgs"
      pattern: "FREE_ORG_ID|PRO_ORG_ID"
    - from: "apps/e2e/src/saas.test.ts"
      to: "mock billing provider (03-02)"
      via: "checkout redirect to /mock-checkout"
      pattern: "mock-checkout"
---

<objective>
Ship the Phase 3 E2E journeys — B1 (billing page), B2 (plan-limit enforcement), B3 (downgrade), O1 (onboarding), A1 (analytics) — extending the proven apps/e2e seed + Playwright pattern, in mock billing mode (BILLING_PROVIDER=mock) against the isolated test DB with AI_PROVIDER=mock.

Purpose: E2E-03 is the phase acceptance requirement. These journeys assert the UI-SPEC testid anchor contract end to end (billing selectable, limits enforced, onboarding + analytics reachable) and catch regressions across the surfaces built in 03-02/03-03/03-05/03-06.

Output: New seed fixtures (free over-limit org, pro org, activity rows, fresh user) and a new saas.test.ts with the five journeys.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-saas-hardening/03-02-SUMMARY.md
@.planning/phases/03-saas-hardening/03-03-SUMMARY.md
@.planning/phases/03-saas-hardening/03-05-SUMMARY.md
@.planning/phases/03-saas-hardening/03-06-SUMMARY.md
@.planning/phases/03-saas-hardening/03-UI-SPEC.md
@apps/e2e/src/seed.ts
@apps/e2e/src/core-loop.test.ts
@apps/e2e/src/prepare-test-db.ts
@apps/e2e/playwright.config.ts
@packages/schemas/src/db/subscription.ts
@packages/schemas/src/plans.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 3 seed fixtures — over-limit free org, pro org, activity rows, fresh user</name>
  <files>apps/e2e/src/seed.ts</files>
  <read_first>
    - apps/e2e/src/seed.ts — current structure (insertUser, TEST_USER/TEST_USER_B, graph fixture) to extend without breaking
    - apps/e2e/src/prepare-test-db.ts — where seedTestData() runs (before the API boots) — the phase 3 fixture must run in the same seedTestData() call
    - packages/schemas/src/db/subscription.ts (03-01) — exact columns for subscription inserts (orgId unique, plan, status, ids nullable)
    - packages/schemas/src/plans.ts — free limits { 2, 5, 50, 500 } the over-limit fixture must exceed
    - packages/schemas/src/db/project.ts, card.ts, proposal.ts, comment.ts, organization-member.ts — columns for activity rows (createdAt/approvedAt must be settable: timestamps are plain columns, pass explicit values)
  </read_first>
  <action>
    Extend apps/e2e/src/seed.ts:
    - Export `FREE_ORG_ID = "00000000-0000-4000-8000-000000000050"` and `PRO_ORG_ID = "00000000-0000-4000-8000-000000000051"` (deterministic constants read by the journeys).
    - Add `seedPhase3Fixtures()` invoked inside seedTestData() (after the existing graph fixture so Phase 1/2 journeys stay green):
      1. OVER-LIMIT FREE ORG (FREE_ORG_ID, name "Over Limit Org"): organization + organizationMember (TEST_USER owner) rows. Insert exactly 2 projects (free projects limit = 2 → usage.projects === limits.projects → banner metric "projects", New board disabled). Add exactly 5 accepted members (TEST_USER + 4 more seeded users) → members = 5 === limit (member metric also at limit; banner shows the FIRST metric in LIMIT_METRICS order → "projects"). Insert 5 cards across the projects (cards under the 500 limit). aiActions: insert exactly 50 proposals this calendar month across the projects (each needs instruction/prompt/aiResponse text; createdBy TEST_USER). Do NOT create a subscription row (absent row = free plan default — proves the free-default path).
      2. PRO ORG (PRO_ORG_ID, name "Pro Org"): organization + organizationMember (TEST_USER owner) + subscription row { plan: "pro", status: "active", orgId: PRO_ORG_ID }. One project, 3 cards, no over-limit state.
      3. ACTIVITY ORG for A1 — reuse TEST_USER's E2E_ORG_ID (from the graph fixture): insert activity rows dated within the last 30 days (explicit createdAt/approvedAt timestamps: 2 extra cards with createdAt 3 and 10 days ago, 2 approved proposals with approvedAt set + status "approved", 3 comments with createdAt set). Totals must be > 0 for all four metrics.
      4. FRESH USER for O1: insertUser(TEST_USER_C, email "e2e-c@test.com", name "E2E User C") with NO orgs and NO projects (credential user exists; journeys sign in and land on /onboarding via the zero-project guard).
    - Keep TEST_USER / TEST_USER_B / graph fixture behavior identical (Phase 1/2 journeys must still pass).
    - All inserts via Drizzle direct (mirroring the existing seed style), orgId/projectId set on every row.
  </action>
  <verify>
    <automated>bun --filter e2e typecheck && bun --filter e2e lint</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter e2e typecheck` and `lint` pass
    - seed.ts exports FREE_ORG_ID + PRO_ORG_ID; seedPhase3Fixtures runs inside seedTestData()
    - Over-limit org: exactly 2 projects, exactly 5 accepted members, exactly 50 proposals dated this month (verified via the B2 journey)
    - Pro org has a subscription row plan "pro"; free orgs have NO subscription row (free-default path)
    - Activity rows carry explicit within-30-day timestamps (grep seed for the date constants)
  </acceptance_criteria>
  <done>Phase 3 fixtures seed deterministically without breaking Phase 1/2 journeys.</done>
</task>

<task type="auto">
  <name>Task 2: B1 billing page + B2 plan-limit enforcement journeys</name>
  <files>apps/e2e/src/saas.test.ts</files>
  <read_first>
    - apps/e2e/src/core-loop.test.ts — the signIn helper + test.describe.serial structure to reuse
    - 03-UI-SPEC.md — the "E2E Visual Anchors" table for B1/B2 (current-plan, plan-grid, plan-card-free/pro, plan-select-pro, current-plan-badge, usage-section, usage-meter-{metric}, usage-bar-{metric} + data-pct, limit-banner + data-limit-metric, limit-banner-upgrade, limit-tooltip) and the B1/B2 journey descriptions
    - apps/e2e/src/seed.ts (Task 1) — FREE_ORG_ID constant for B2
  </read_first>
  <action>
    Create apps/e2e/src/saas.test.ts with a `test.describe.serial("phase 3", () => { ... })` block. Reuse the module-level signIn helper pattern from core-loop.test.ts (import TEST_USER, TEST_USER_C, FREE_ORG_ID, PRO_ORG_ID from "./seed"). saas.test.ts runs under the same webServer (API in mock billing mode — BILLING_PROVIDER defaults to mock) and the same isolated test DB.

    B1 — Billing page (TEST_USER, free default org E2E_ORG_ID):
    1. signIn(page, TEST_USER.email, TEST_USER.password).
    2. page.goto(`/orgs/${E2E_ORG_ID}/billing`).
    3. expect current-plan visible with text "Free"; expect current-plan-badge on plan-card-free; plan-card-pro visible with plan-select-pro.
    4. Usage meters: expect usage-section visible; expect at least one usage-bar-{metric} with a data-pct attribute (assert the attribute parses as a number >= 0).
    5. Upgrade determinism (mock provider): click plan-select-pro → expect the button shows "Redirecting…" → page navigates to the returned mock checkout url — `await page.waitForURL(/mock-checkout/)` (the mock provider returns `${CLIENT_URL}/mock-checkout?orgId=...`; the catch-all route renders the landing page, which is fine — the assertion is the redirect happened). Do NOT run a real checkout (mock mode has no payment flow; the pro state for B3 comes from the seeded pro org).
    6. Back: page.goto(`/orgs/${E2E_ORG_ID}/billing`) → meters still render (state unchanged — checkout was never completed).

    B2 — Plan-limit enforcement (TEST_USER, FREE_ORG_ID over-limit org):
    1. signIn(page, TEST_USER.email, TEST_USER.password).
    2. page.goto(`/orgs/${FREE_ORG_ID}/projects`) — if the page requires selecting the org first (org switcher sets selectedOrgId in the board store), click the org switcher and choose "Over Limit Org" before asserting.
    3. expect limit-banner visible with data-limit-metric="projects" and copy "You've reached your free project limit."
    4. expect the "New board" button (projects.tsx) to be disabled and the limit-tooltip testid present (assert the trigger; the tooltip text "Limit reached — upgrade to Pro" appears on hover — optional hover assertion).
    5. page.goto(`/orgs/${FREE_ORG_ID}/billing`) → expect usage-bar-projects with data-pct="100"; expect the projects meter caption "Limit reached — upgrade to Pro to continue."
    6. limit-banner-upgrade: navigate to `/orgs/${FREE_ORG_ID}/projects`, click limit-banner-upgrade → expect URL becomes `/orgs/${FREE_ORG_ID}/billing`.
    7. Server enforcement spot-check: POST /api/projects via page.request with body { orgId: FREE_ORG_ID, name: "Too many" } → expect 402 with data.code "limit_reached" (page.request shares the session cookies).
  </action>
  <verify>
    <automated>bun --filter e2e test -- src/saas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter e2e typecheck` passes
    - B1 passes: Free badge on free card, plan-select-pro visible, upgrade click redirects to /mock-checkout, meters render data-pct
    - B2 passes: banner with data-limit-metric=projects + exact copy, New board disabled + limit-tooltip, usage-bar-projects data-pct=100 + destructive caption, banner CTA navigates to Billing, API POST /api/projects returns 402 limit_reached
  </acceptance_criteria>
  <done>B1 + B2 prove billing page and server-truth limit enforcement end to end.</done>
</task>

<task type="auto">
  <name>Task 3: B3 downgrade + O1 onboarding + A1 analytics journeys</name>
  <files>apps/e2e/src/saas.test.ts</files>
  <read_first>
    - 03-UI-SPEC.md — E2E anchors for B3 (plan-select-free, plan-change-dialog, plan-change-confirm, plan-change-cancel), O1 (onboarding-welcome/start/template/skip, onboarding-template-blank/product-launch), A1 (analytics-stat-{metric}, analytics-chart-{metric}, analytics-bar-{metric}-{index} + data-value, analytics-empty-cta, analytics-retry) and the journey descriptions
    - apps/e2e/src/seed.ts — PRO_ORG_ID, TEST_USER_C, E2E_ORG_ID constants
    - apps/e2e/src/core-loop.test.ts — signIn helper + how toast assertions are done in this repo (if any)
  </read_first>
  <action>
    Append the three remaining journeys inside the same `test.describe.serial("phase 3")` block (serial mode — reuse the signed-in session where convenient, re-running signIn for determinism):

    B3 — Downgrade flow (TEST_USER, PRO_ORG_ID):
    1. signIn(TEST_USER). page.goto(`/orgs/${PRO_ORG_ID}/billing`).
    2. expect current-plan shows "Pro"; plan-select-free visible on plan-card-free.
    3. Click plan-select-free → expect plan-change-dialog visible with title "Downgrade to Free?".
    4. Click plan-change-cancel → dialog closes; expect plan-select-free still visible and current-plan still "Pro" (no change).
    5. Reopen: click plan-select-free → click plan-change-confirm → expect success toast "You're now on the Free plan." (poll for the toast element) → expect current-plan to show "Free" and plan-card-free to now carry current-plan-badge (plan flipped — useDowngrade invalidated billing and refetched).

    O1 — Onboarding (fresh users):
    1. signIn(TEST_USER_C) (no orgs, no projects). Land on `/dashboard` → expect redirect to `/onboarding` (the protected-route guard fires — waitForURL(/onboarding/)).
    2. expect onboarding-welcome visible with "Welcome to Storyteller"; click onboarding-start → expect onboarding-template visible.
    3. Click onboarding-template-blank → expect navigation to `/projects/{slug}` (waitForURL(/projects/)) and a board page renders (project created via useCreateProject).
    4. Second fresh user (skip path): signIn with a fresh seeded user or reuse TEST_USER_C after clearing sessionStorage — simplest deterministic approach: page.evaluate(() => sessionStorage.clear()) then page.goto("/dashboard") — but the user now has a project, so the guard would not fire. Instead: seed a second fresh user TEST_USER_D (email "e2e-d@test.com") in Task 1 with no orgs/projects; signIn(TEST_USER_D) → expect redirect to /onboarding → click onboarding-skip → expect URL becomes /projects and no further redirect to /onboarding within a 5s settle window (expect(page).toHaveURL(/projects/) and assert no onboarding redirect: wait 2s, expect URL still /projects).

    A1 — Analytics:
    1. signIn(TEST_USER). page.goto(`/orgs/${E2E_ORG_ID}/analytics`).
    2. expect analytics-stat-cardsCreated, analytics-stat-proposalsApproved, analytics-stat-commentsPosted, analytics-stat-activeMembers visible; poll each value text to be a number > 0 (fixture guarantees activity).
    3. expect analytics-chart-cardsCreated/proposalsApproved/commentsPosted svg elements visible; each has at least one rect `[data-testid^="analytics-bar-"]` (count >= 1) with a data-value attribute.
    4. Empty case: fresh org for TEST_USER (a brand-new personal org has zero activity) — create it via the org switcher or POST /api/orgs via page.request (body { name: "Empty Org" }) then page.goto(`/orgs/${newOrgId}/analytics`) → expect analytics-empty-cta visible; click it → lands on /projects.

  </action>
  <verify>
    <automated>bun --filter e2e test -- src/saas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun --filter e2e typecheck` passes
    - B3 passes: dialog opens/closes, cancel makes no change, confirm flips plan to Free with the success toast
    - O1 passes: fresh user lands on /onboarding; blank template creates a project and navigates to the board; skip lands on /projects with no re-redirect
    - A1 passes: 4 stat values > 0, 3 charts with >= 1 bar each, empty-cta on a zero-activity org
    - Full suite: `bun --filter e2e test` (core-loop J1-J4 + phase-2 J1-J3 + phase-3 B1/B2/B3/O1/A1 + 5 smoke tests) passes
  </acceptance_criteria>
  <done>B3/O1/A1 green; the full E2E suite passes (E2E-03 verified).</done>
</task>

</tasks>

<artifact_production>

## Artifacts This Plan Produces

**Modified:**

- `apps/e2e/src/seed.ts` → + `FREE_ORG_ID`, `PRO_ORG_ID`, `TEST_USER_C`/`TEST_USER_D`, `seedPhase3Fixtures` (over-limit org, pro org, activity rows)
- `apps/e2e/src/saas.test.ts` → new `test.describe.serial("phase 3")` with B1/B2/B3/O1/A1 journeys
</artifact_production>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → app UI/API | Playwright drives the real browser + API against the isolated test DB — the journeys exercise the production trust boundaries (auth, org scope, plan limits, billing) |
| E2E test → API (page.request) | The 402 spot-check + org creation use the browser context's session — legitimate authenticated paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-60 | Spoofing | seeded users sign-in | mitigate | Seed follows the Phase 1 pattern exactly (providerId "credential", scrypt hash, emailVerified true) — a fixture that cannot sign in proves nothing |
| T-03-61 | Tampering | test data pollution | mitigate | Isolated per-run test DB (created/migrated/seeded in the API webServer command, dropped in global teardown — 01-08 pattern); fixtures are org-scoped under deterministic ids |
| T-03-62 | Info Disclosure | fixture scope | mitigate | Every fixture row carries orgId/projectId; over-limit + pro orgs are distinct orgs so cross-org leakage would fail B2/A1 assertions |
| T-03-63 | DoS | mock checkout redirect | accept | The /mock-checkout URL is a dev/test-only mock-provider artifact (03-02 T-03-15); production uses real Stripe URLs |
| T-03-SC | Tampering | npm installs | accept | No installs in this plan |

</threat_model>

<verification>
- `bun --filter e2e typecheck` + `lint` pass
- `bun --filter e2e test` passes — full suite: core-loop J1-J4, phase-2 J1-J3, phase-3 B1/B2/B3/O1/A1, 5 smoke tests (requires `docker compose up -d postgres mailpit` first; API webServer prepares the isolated test DB with AI_PROVIDER=mock + BILLING_PROVIDER=mock)
- The seed additions keep Phase 1/2 journeys green (verified by the same full-suite run)
</verification>

<success_criteria>

- B1: billing page renders per contract; upgrade click redirects to the hosted (mock) checkout url
- B2: banner + disabled action + 100% meter + server 402 all proven on the over-limit org
- B3: downgrade dialog cancel/confirm flow flips the pro org to Free
- O1: zero-project users hit onboarding; blank template creates a board; skip has no loop
- A1: analytics stats + charts render seeded activity; empty state on a fresh org
- All 17+ E2E tests pass against the isolated test DB with mock AI + mock billing
</success_criteria>

<output>
Create `.planning/phases/03-saas-hardening/03-07-SUMMARY.md` when done
</output>

